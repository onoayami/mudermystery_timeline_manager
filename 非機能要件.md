# マダミス時系列整理アプリ 非機能要件

本書は「マダミス時系列整理アプリ」（単一 HTML ファイル）の**非機能要件**を、機能面以外の品質・制約の観点からまとめたものである。
対象は [機能要件.md](機能要件.md) に定義された機能を実現するための、性能・信頼性・保守性・セキュリティ・運用などの要求水準とする。

---

## 1. アーキテクチャ / 技術制約

- **単一 HTML ファイル構成**：1 つの `.html` に HTML・CSS・JavaScript をすべて内包する。
- **外部依存ゼロ**：CDN・npm パッケージ・フレームワーク・Web フォント等の外部リソースを一切参照しない。
  - オフライン環境・社内ネットワーク・ファイル直開き（`file://`）でも完全に動作すること。
- **Vanilla JavaScript のみ**：ビルド工程（トランスパイル・バンドル）を必要としない。ファイルをそのままブラウザーで開けば動く。
- **サーバー不要**：バックエンド・API・データベースを持たないクライアント完結型アプリ。
- **状態管理**：グローバル変数と `localStorage` による素朴な状態保持とし、外部状態管理ライブラリを導入しない。

---

## 2. 動作環境 / 互換性

- **対象ブラウザー**：モダンブラウザーの最新版（Chrome / Edge / Firefox / Safari）を主対象とする。
- **利用 API**：`localStorage`、`Blob` / `URL.createObjectURL`、`FileReader`、CSS `position: sticky` / `zoom` などモダンブラウザー標準機能を前提とする。
- **デバイス対応**：PC（マウス操作）とスマートフォン／タブレット（タッチ操作）の双方で利用可能とする。
- **文字コード**：UTF-8。言語は日本語（`<html lang="ja">`）。
- **後方互換**：旧データ形式を読み込み時に自動移行する。
  - 単数 `character` を配列 `characters` へ移行。
  - フラグ絞り込みの旧 `true/false` 形式を `all/flag/noflag` へ移行。

---

## 3. レスポンシブ / UI 要件

- **2 つの表示モード**：PC版（横並び）とスマホ版（縦積み）を明示的に切り替え可能とし、初回は画面幅 900px を境に自動判定する。
- **レイアウトの自動適応（view-compact）**：PC版選択中でも画面幅が狭くモード切替ボタンが折り返る場合は、ユーザーの選択を変えずに時系列部分だけをスマホ表示へ自動切替え、レイアウト崩れを防ぐ。
- **操作要素のタッチ配慮**：ボタン・チェックボックス・入力欄はタッチでも扱えるサイズ・間隔を確保する。
- **横スクロール対応**：人物比較テーブルは列数が多い場合に横スクロールでき、時間列・見出し行は `sticky` で固定表示する。
- **視認性**：ダークテーマ（濃紺背景）を基調とし、死亡推定時刻の赤線・フラグの金線など重要情報は背景に埋もれない配色とする。
- **色の可読性**：キャラクター色に応じて文字色（黒／白）を自動選択し、コントラストを確保する。
- **表示倍率**：人物比較ビューは 10%〜200% のズームに対応し、情報量が多い場合でも一覧性を調整できる。

---

## 4. 性能要件

- **即時応答**：ローカル完結のため、カード追加・編集・並べ替え・表示切替は原則として体感的に即時（画面のちらつきや待ちを感じさせない）であること。
- **書き込みのデバウンス**：秘密のメモは入力のたびに保存せず、約 300ms のデバウンスで書き込み負荷を抑える。
- **想定データ規模**：1 セッション（数十〜数百件程度のイベント、十数名程度のキャラクター）で快適に動作すること。
- **再描画コスト**：状態変更時は必要範囲を再描画する方針とし、大量データでも過度な遅延を生まないよう努める。

---

## 5. データ永続化 / 信頼性

- **自動保存**：ユーザー操作による変更は都度 `localStorage` に保存し、明示的な「保存」操作を不要とする。
- **保存キーの一元管理**：すべての保存キーを `KEYS` オブジェクトに集約し、キー名の打ち間違いによる不整合を防ぐ。
- **障害時の継続性（フェイルセーフ）**：
  - 読み込み失敗（データ破損・JSON パース失敗）時は、例外を握りつぶして初期値へフォールバックし、アプリを停止させない。
  - 保存失敗（容量超過・プライベートモード）時は処理を継続し、**初回のみ**ユーザーへ警告を表示する（警告の多重表示を防ぐ）。
- **バックアップ / 移行**：JSON 形式での書き出し・取り込みにより、端末間移動やバックアップを可能とする。
  - 取り込みファイルは `app: 'madamisu'` 判定・既知キーのフィルタリングで検証し、不正・無関係なファイルを拒否する。
  - 取り込みは既存データを置き換える破壊的操作であるため、確認ダイアログを必須とする。

---

## 6. データ保護 / プライバシー

- **ローカル完結**：入力データ（推理メモ・ネタバレ含む）は一切外部送信されず、ブラウザー内 `localStorage` のみに保存される。
- **ネタバレ配慮**：秘密のメモやカード内容を隠す「非表示（👁️）」機能を備え、画面共有・のぞき見時の情報漏えいを軽減する。
- **端末依存の保存**：データは保存した端末・ブラウザーに紐づく。共有端末やプライベートモードでは永続化されない場合があることを許容する。
- **削除操作の明示**：全データ削除・全キャラクター削除・全タイムライン削除・取り込みによる置換は、いずれも確認ダイアログを経てから実行する。

---

## 7. セキュリティ要件

- **XSS 対策**：ユーザー入力（キャラクター名・できごと・メモ等）を DOM に描画する際は、エスケープ処理（`escapeHtml` 等）を通し、スクリプト注入を防ぐ。
- **信頼できない入力の検証**：取り込み JSON はアプリ識別子・キーの妥当性を検証し、想定外の値は取り込まない。
- **外部通信なし**：ネットワークリクエストを行わないため、通信経路上の情報漏えい・改ざんリスクを構造的に排除する。
- **権限最小**：Cookie・位置情報・カメラ等の機微な権限を要求しない。

---

## 8. ユーザビリティ / アクセシビリティ

- **学習コスト低減**：「？」ヘルプモーダルで基本操作・各ボタンの意味・応用（期間登録、空欄登録の挙動など）を解説する。
- **入力補助**：時間欄は全角→半角変換・「:」自動挿入・正規化により、フォーマットを意識せず入力できる。時間バッジはクリックでその場編集できる。
- **誤操作防止**：破壊的操作（全削除・取り込みによる置換）は確認ダイアログを備え、削除確認の有無はユーザーが設定できる。キャラクターはロック（🔓/🔐）で名前・色の編集と削除を防止できる。セッション名の確定は Enter 2 回または✓で行い、意図しない確定を防ぐ。
- **状態の可視化**：トグル類（フラグ・非表示・死亡推定 ON/OFF・並び固定・ロックなど）は見た目で現在状態が判別できる。
- **補助属性**：折りたたみトグル等には `aria-expanded` 等の属性を付与し、状態を機械可読にする。

---

## 9. 保守性 / コード品質

- **関心の分離**：入力補正・時刻正規化・描画・保存などを目的別の関数へ分割する。
- **コメント**：日本語コメントで意図・非自明な仕様（コネクタ描画、カーソル位置維持など）を説明する。
- **定数化**：パレット色・ズーム範囲などの値を定数として集約し、変更容易性を高める。
- **単一ファイルの可搬性**：ファイルを配布・複製するだけで同一挙動を再現でき、環境構築を不要とする。

---

## 10. 運用 / 配布

- **配布容易性**：HTML ファイルを共有・ダウンロードするだけで利用開始できる。インストール・アカウント登録は不要。
- **更新方法**：新しい HTML ファイルへ差し替えることで更新する。既存データは `localStorage` に残るため、原則として引き継がれる。
- **可用性**：外部サービスに依存しないため、提供側のサーバー障害等による停止が発生しない。

---

## 11. 制約・前提事項

- **保存容量**：`localStorage` の容量上限（ブラウザー依存、概ね数 MB）に従う。上限超過時は保存に失敗し警告を表示する。
- **同時編集非対応**：単一端末・単一利用者を前提とし、複数端末間のリアルタイム同期は行わない（同期はエクスポート／インポートによる手動運用）。
- **データの揮発性**：ブラウザーのデータ削除・プライベートモード・端末変更によりデータが失われうるため、重要データは書き出しでのバックアップを推奨する。
- **印刷 / 出力**：帳票印刷や PDF 出力は要件外（ブラウザー標準機能の利用は妨げない）。
