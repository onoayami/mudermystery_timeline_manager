# マダミス専用メモアプリ（マダミス時系列整理.html）非機能要件書

※「一見非機能要件でも挙動の再現に必要な情報（色・寸法・レイアウト規則など）」は機能要件書（機能要件.md）側に記載している。本書には、それ以外の品質特性に関する要件のみをまとめる。

## 1. 実行環境・可搬性
- 単一のHTMLファイルのみで完結し、ビルド・サーバー・外部ライブラリ・CDN・ネットワーク通信を一切必要としない。`file://` で直接開いても全機能が動作すること。
- 対応OS/ブラウザ: Windows / macOS / iOS / Android の主要モダンブラウザ（Chrome, Safari, Edge, Firefox）。最低バージョンの目安は **Chrome / Edge 80以降、Safari 13.1以降、Firefox 74以降**（これより古いブラウザでの動作は保証しない。Optional chaining 等の新しい構文は使わない方針だが、CSS等の互換性はこの範囲で確認する）。
- 文字コードは UTF-8、言語は日本語（`lang="ja"`）。

## 2. データ永続性・信頼性
- 保存先はブラウザの localStorage のみ。端末・ブラウザをまたいだ同期は行わない（移行はエクスポート/インポート機能で行う）。`file://` 運用ではHTMLファイルの移動・改名で保存データが見えなくなるブラウザがあるため、README・アプリ内ヘルプで事前バックアップ（書き出し）を案内する。
- localStorage の読み込み失敗（破損JSON等）でアプリが起動不能にならないこと（フォールバック値で初期化）。
- 書き込み失敗（容量超過・プライベートモード）でも操作が継続できること。警告アラートはセッション中1回のみ。
- インポートは失敗時に元データへ復元できること（事前バックアップ→失敗時ロールバック）。
- 旧バージョンのデータ形式（`character` 単数、フラグ絞り込みの true/false）を読み込めること（後方互換）。

## 3. 性能
- イベント数百件程度でも操作が体感で遅延しないこと。
- フラグ・非表示・色隠しの切替は、可能な限り全再描画を避けて対象カードのみ部分更新する。
- カード右端揃えの計測は「読み取り一括→書き込み一括」で行い、レイアウトスラッシングを避ける。
- メモの自動保存は 300ms のデバウンスとし、ページを離れる直前（pagehide / visibilitychange の hidden 時）には即座に書き込んで取りこぼしを防ぐ。リサイズ処理は requestAnimationFrame でのデバウンス＋横幅変化時のみ再計算とする。
- 比較ビューのセル描画では `events.indexOf` 等の O(n) 探索を避け、事前に Map を構築する。

## 4. セキュリティ
- ユーザー入力を HTML に埋め込む際は必ずエスケープする（XSS対策）。
- 色値・CSSセレクタへの差し込みは検証済みの値のみ（CSSインジェクション対策）。
- 外部通信を行わないため、データが端末外へ送信されないこと。

## 5. ユーザビリティ・アクセシビリティ
- 絵文字・記号がOS間で豆腐化・字形崩れしないよう、フォントスタックと異体字セレクタ（FE0F/FE0E）を統一する。
- 破壊的操作（各種一括削除・インポート・カード削除）には確認ダイアログを設ける（カード削除のみ設定でOFF可）。
- 開閉トグルには `aria-expanded` を付与する。主要ボタンには `title` によるツールチップを付ける。
- タッチ端末でも「外側タップでポップアップを閉じる」操作が機能すること。

## 6. 保守性
- localStorage キーは定数オブジェクト `KEYS` に集約する。
- 追加フォームのリセット処理は `resetAddEventForm()` に集約する。
- CSS は用途別にコメントで区分し、マジックナンバー（コネクター寸法など）には根拠コメントを付ける。
