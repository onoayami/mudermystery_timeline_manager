# index→ID化 段階的改修計画書（AI実行用）

- **作成日**: 2026-07-25
- **対象ファイル**: マダミス時系列整理.html（単一HTML・Vanilla JS・localStorage永続化）
- **参照資料**: コードレビュー結果_第5回.md（指摘7）、機能要件.md、エラー集.md（特に13・15・25・32）
- **方針**: 「ルールの明文化（案A）」を先に行い、その後「名札（ID）を配ってから少しずつ呼び方を切り替える（案B）」を段階実行する。**各フェーズは単独で完結し、途中でやめてもアプリは常に正常動作する。**

---

## 1. 背景と目的

### 何が問題か（1分で分かる説明）

現在、カードの削除・編集などのボタンには「上から何番目のカードか（index＝座席番号）」が書き込まれている。並べ替えが起きた直後に古い番号で操作すると**別のカードを操作してしまう**構造的リスクがある。今は「並びが変わったら必ず画面全体を描き直す」という暗黙ルールで無事故だが、これは*たまたま守られている*状態（レビュー指摘7）。

### ゴール

- 各カード・各キャラクターに**不変のID**を持たせ、操作対象をIDで特定する方式へ移行する。
- 移行中・移行後も**機能要件.mdに書かれた振る舞いを一切変えない**（IDは完全に内部の仕組み）。

### 非ゴール（やらないこと）

- UI・見た目・操作方法の変更
- 部分更新（refreshCard）の適用範囲拡大などの性能改善（ID化完了後の別案件とする）
- localStorage のキー名変更

---

## 2. 現状の実測データ（2026-07-25 時点のコード調査）

| 項目 | 実測値 |
|---|---|
| indexを焼き込んだ onclick | 21箇所・15種類の操作 |
| index を引数に取る関数 | 約20個（events系・characters系） |
| `events[index]` 直接参照 | 25箇所 |
| `characters[index]` 直接参照 | 10箇所 |
| `sortEvents()` を呼ぶ関数 | addEvent / saveEditTime / saveEditEvent / saveCompareEdit / moveUnknownEvent ほか（L815, 1810, 1876, 1931, 1976, 2004） |
| 部分更新 `refreshCard(index)` | 3箇所から呼ばれる（L1832, 1840, 1847。いずれも単一カードのトグル操作） |
| 名前ベースの識別 | `compareOrder`（比較ビューの列順）はキャラ**名**で保存。名前変更時に追従コードあり（L1241） |

---

## 3. メリット・デメリット精査（この段階方式について）

### メリット

1. **人違いバグの根絶**: index のズレによる誤削除・誤編集が構造的に起きなくなる。
2. **将来の改修が安全になる**: 部分更新の拡大、アンドゥ機能、カードの手動並べ替え等、「並び順と識別が絡む機能」を安心して追加できる土台になる。
3. **compareOrder の名前依存も解消可能**: キャラ名変更時の追従処理（バグの温床）を将来的に削除できる。
4. **各フェーズが小さい**: 1フェーズあたりの変更は数行〜十数箇所。エラー集13・15で記録された「大規模一括置換の部分失敗」事故を構造的に避けられる。
5. **いつでも中断可能**: どのフェーズの直後でもアプリは完全動作する。「Phase 1 だけやって寝かせる」も有効な選択。

### デメリット・リスク

1. **見た目が何も変わらない**: 労力に対して体感できる改善がない。モチベーション維持が課題。
2. **フェーズ間の中途半端な状態が長引くリスク**: index方式とID方式が混在する期間は、コードを読む人が「どちらが正か」迷う。→ 対策: 各フェーズでコメントに現状を明記（Phase 0 参照）。
3. **書き出しJSONにフィールドが増える**: `id` が追加される。旧バージョンのアプリはunknownフィールドを無視して読むため実害はないが、Phase 1 の確認項目で検証する。
4. **ID生成の環境依存**: `crypto.randomUUID()` は対象下限ブラウザ（iOS 13.4 Safari）に存在しない。→ フォールバック関数を必ず用意する（Phase 1 のコード参照）。
5. **テスト工数**: 各フェーズ後にスモークテスト（§6）が必要。ただし1回あたり10分程度。

### 総合判断

一括改修（50箇所超を一度に変更）と比べ、段階方式は**総作業量はやや多いが、事故確率と事故時の被害が桁違いに小さい**。エラー集に一括置換事故の前歴がある本プロジェクトでは段階方式が妥当。

---

## 4. AIに渡すときの共通ルール（全フェーズ共通・毎回プロンプトに添付すること）

```
【共通ルール】
- 対象は マダミス時系列整理.html のみ。他のファイルは変更しない（指示がある場合を除く）。
- 機能要件.md に書かれた振る舞いを一切変えない。UI・文言・レイアウトも変えない。
- 置換は「小さく・1箇所ずつ」。一括置換で複数箇所を同時に変えない（過去に部分失敗の事故あり）。
- 編集前に必ず対象箇所の現在のコードを読み、実際のテキストをコピーして置換に使う。
- 行番号はコードの変更でズレている可能性がある。行番号ではなく関数名・コード内容で対象を特定する。
- 編集後、grep等でディスク上の実体を確認する（エディタのバッファを信用しない）。
- このフェーズの指示に書かれていない「ついでの改善」は一切しない。気づいた点は報告のみ。
- 完了したら、変更箇所の一覧（関数名と変更内容）を報告する。
```

**人間側の作業（毎フェーズ共通）**: 開始前に `git status` がクリーンであることを確認 → フェーズ完了・動作確認後に `git commit` → 問題があれば `git checkout -- マダミス時系列整理.html` で即ロールバック。

---

## 5. フェーズ計画

### Phase 0: ルールの明文化と現状固定（案A・リスクゼロ）

**目的**: 移行中の安全網として、暗黙ルールをコードに明記する。

**AIへの指示文（そのまま貼る）**:

```
マダミス時系列整理.html に以下のコメント整備だけを行ってください。コードの動作は一切変えないでください。
1. sortEvents() 関数の直前コメントを確認し、なければ以下を追記:
   「【重要ルール】sortEvents() を呼んだ後は必ず renderEvents()（全再描画）を呼ぶこと。
    sort後に refreshCard() 等の部分更新を呼ぶと、DOMに焼き込まれた index が
    ズレて別のカードを操作するバグになる。（レビュー第5回・指摘7）
    → 根本対策として ID 化を段階的に実施中。ID化段階改修計画書.md 参照」
2. refreshCard() 関数の直前に追記:
   「【重要ルール】この関数は並び順が変わらない操作（フラグ・非表示・色のトグル）専用。
    並び順が変わりうる操作の後で呼んではならない。」
3. 既存のコメント（L1764付近の index ズレ注意コメント）と重複する場合は統合して1つにする。
```

**確認事項**:
- [ ] `git diff` でコメント行以外の変更がないこと
- [ ] ブラウザで開いて起動すること（コメント内の記号による構文破壊がないか）

---

### Phase 1: 名札の配布（データにIDを付ける・動作は不変）

**目的**: すべてのできごと（events）に不変IDを持たせる。**呼び方（index）はまだ変えない。**

**AIへの指示文**:

```
マダミス時系列整理.html に以下の3点だけを実装してください。既存の動作・表示は一切変えないでください。

1. ID生成ヘルパーを追加（safeGet などのヘルパー群の近くに配置）:
   function genId() {
     // crypto.randomUUID は iOS 13.4 等の対象下限ブラウザに存在しないためフォールバック必須
     try { if (window.crypto && crypto.randomUUID) return crypto.randomUUID(); } catch (e) {}
     return 'ev-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2, 10);
   }

2. 読み込み時マイグレーション: 既存の後方互換処理（ev.characters を補う events.forEach ブロック）の直後に追加:
   // IDのないカード（旧データ・旧書き出しファイル）にはその場でIDを発行する
   events.forEach(ev => { if (!ev.id || typeof ev.id !== 'string') ev.id = genId(); });
   // 手編集されたJSON等によるID重複を矯正する
   const seenIds = new Set();
   events.forEach(ev => { if (seenIds.has(ev.id)) ev.id = genId(); seenIds.add(ev.id); });
   ※マイグレーション後に saveEvents() 相当の保存を1回行うこと（既存の保存ヘルパーを使う）。

3. addEvent() 内のイベントオブジェクト生成（const ev = { time, text, ... }）に id: genId() を追加。
```

**作りながら確認すべき事項**:
- [ ] `file://` で開いた時に `crypto.randomUUID` が使える環境・使えない環境の両方でIDが生成されるか（コンソールで `genId()` を数回実行し、形式と重複がないことを確認）
- [ ] マイグレーションの保存タイミングがプライベートモード（保存失敗環境）でも例外で停止しないか（safeSet系ヘルパー経由になっているか）

**バグ確認（スモークテスト §6 に加えて）**:
- [ ] 旧データ（ID無し）が入った状態でリロード → 全カードにIDが付与される（コンソールで `events` を確認）
- [ ] **旧バージョンの書き出しJSON**（IDフィールド無し）を取り込み → 正常に読み込まれ、リロード後にIDが付与される
- [ ] 新バージョンで書き出したJSONに `id` が含まれる／再取り込みでIDが**保持**される（ラウンドトリップ）
- [ ] （可能なら）**旧バージョンのHTML**に新JSONを取り込み → unknownフィールドは無視され正常動作する

---

### Phase 2: 単純操作のID化（削除・トグル系）

**目的**: 誤対象時の被害が大きい**削除**と、単純なトグル操作をID呼びに切り替える。

**方式（ラッパー方式・既存関数は温存）**:

```js
// IDからindexを解決する共通ヘルパー
function evIndexById(id) { return events.findIndex(ev => ev.id === id); }

// 例: 削除
function deleteEventById(id) {
  const index = evIndexById(id);
  if (index === -1) return; // 対象消失時は何もしない（誤操作より安全）
  deleteEvent(index);       // 既存関数を再利用し、差分を最小にする
}
```

**AIへの指示文**:

```
マダミス時系列整理.html に evIndexById ヘルパーを追加した上で、以下の操作だけを ID 経由に切り替えてください。
対象: deleteEvent（onclick 2箇所）、toggleEventFlag（2箇所）、toggleEventConceal（2箇所）、toggleEventColor（1箇所）
方法:
- 各関数に xxxById(id) ラッパーを追加（deleteEventById の例に倣う）。既存の index 版関数は変更しない。
- renderEvents / refreshCard 内のカードHTML生成で、対象の onclick="deleteEvent(${index})" を onclick="deleteEventById('${ev.id}')" の形式に書き換える。
- ev.id は genId() 生成（英数字とハイフンのみ）なのでエスケープ不要だが、念のためテンプレートに埋め込む前に String(ev.id).replace(/[^a-zA-Z0-9_-]/g, '') を通すサニタイズヘルパーを作って使うこと。
- 1操作ずつ（deleteEvent → flag → conceal → color の順）編集し、都度 grep で置換結果を確認すること。
```

**作りながら確認すべき事項**:
- [ ] `refreshCard()` が再生成するHTML断片にも同じ onclick があるはず。**renderEvents側とrefreshCard側の両方**が切り替わっているか（片方だけだと再描画のたびに新旧が入れ替わる）→ `grep -n "deleteEvent(" ` で index 版の残存箇所を確認
- [ ] 比較ビュー側のカードにも同種のボタンがないか横断確認

**バグ確認**:
- [ ] カードを3枚作成 → 真ん中を削除 → **正しいカードが消える**
- [ ] 削除確認ダイアログON/OFF両方で動作
- [ ] フラグ・非表示・色トグルが正しいカードに効く（トグル後に別カードを操作しても対象がズレない）
- [ ] 時刻の異なるカードを追加して並び順が変わった直後に削除・トグル → 正しい対象に効く
- [ ] リスト表示・比較表示・スマホ版の3画面で上記を繰り返す

---

### Phase 3: 編集系のID化（フォームを持つ操作）

**目的**: 編集フォームに index を埋め込んでいる操作（レビューが特に危険視した箇所）をID化する。

**対象**: startEditEvent / saveEditEvent / startEditTime / saveEditTime / startCompareEdit / saveCompareEdit / moveUnknownEvent / renderCardCharPopup / toggleCardChar

**AIへの指示文**:

```
Phase 2 と同じラッパー方式で、編集系の操作を ID 経由に切り替えてください。
注意点:
- startCompareEdit / saveCompareEdit は編集フォームのHTMLに index を埋め込んでいる。ここは index ではなく id を埋め込むように変更する（フォームを開いたまま別カードを削除しても保存先がズレないことがこのフェーズの主目的）。
- save系関数の冒頭で evIndexById(id) が -1 の場合（編集中に対象が削除された場合）は、alert('対象のカードが見つかりませんでした') を出して編集フォームを閉じる。
- moveUnknownEvent(index, dir) は第2引数があるため moveUnknownEventById(id, dir) とする。
- 1関数ペア（start/save）ずつ編集・確認する。
```

**バグ確認**:
- [ ] ✏️編集 → 保存／↩︎取消 が正しいカードに効く
- [ ] 時間編集で並び順が変わるケース（10:00→23:00等）で正しく再配置される
- [ ] **危険シナリオの再現テスト**: 比較ビューでカードAの編集フォームを開く → リスト表示に切り替えず、別カードBを削除 → カードAの編集を保存 → **Aに保存される（従来はズレの可能性があった）**
- [ ] 不詳カード（自由記述時刻）の⬆⬇移動が正しく動く
- [ ] カードの人物ポップアップ（登場人物の付け外し）が正しいカードに効く

---

### Phase 4（任意）: キャラクターのID化と compareOrder の名前依存解消

**目的**: characters にもIDを付与し、比較ビューの列順（compareOrder）を名前ベースからIDベースに移行。キャラ名変更時の追従コード（L1241付近）を不要にする。

**実施判断の目安**: Phase 3 まで安定稼働して1〜2週間問題がなければ着手。急がない。

**要点**:
- characters への ID 付与とマイグレーションは Phase 1 と同型。
- compareOrder の保存値マイグレーション: 読み込み時、各要素がキャラIDに一致しなければ「名前→ID」変換して保存し直す。localStorage キー名は変えない。
- 旧書き出しJSON（名前ベースのcompareOrder）の取り込み→リロード時にも同じマイグレーションが効くことを確認。
- 名前変更追従コードは、マイグレーション実装が安定するまで**削除せず残す**（二重に動いても無害な設計にする）。

**バグ確認**:
- [ ] 比較ビューの列⬅➡移動 → キャラ名を変更 → 列順が維持される
- [ ] 列順を並べ替えた状態で書き出し→取り込み→列順が復元される
- [ ] 「並びを固定」トグルON/OFFの両方で動作

---

### Phase 5: 仕上げ（掃除とドキュメント反映）

**目的**: 混在状態を解消し、次の開発者（未来の自分・AI）が迷わない状態にする。

**内容**:
- `grep` で index 焼き込みの onclick が**ゼロ**であることを確認（残っていれば個別に対応）。
- 使われなくなった index 直呼びの経路があれば削除（ラッパーに一本化）。ただし内部で index を使う実装自体は残って良い（入口がIDなら安全）。
- Phase 0 で書いた「sortEvents後は全再描画」コメントを現状に合わせて更新（「ID化済みのため誤対象は起きないが、表示の一貫性のため全再描画の規律は維持」等）。
- 機能要件.md に1行追記: 「各カード・キャラクターは内部的に不変IDで識別される（表示には影響しない）」。
- エラー集.md に今回の移行で得た知見を追記。

---

## 6. スモークテスト（全フェーズ共通・各フェーズ完了時に実施）

所要10分。**PC版・スマホ版の両方**で実施する。

1. [ ] ページを開いて起動する（白画面・コンソールエラーなし）
2. [ ] キャラクター追加（2人以上・色選択）→ 名前編集 → ロック → 色変更が防がれる → 解除
3. [ ] カード追加: 時刻あり／自由記述（「夕方」）／期間モード（開始<終了・開始>終了の日跨ぎ）
4. [ ] 並び順: 時刻順に整列し、自由記述が末尾に来る
5. [ ] カードの ✏️編集・フラグ・非表示・削除
6. [ ] 比較ビュー切替 → 列の⬅➡移動 → ズーム変更 → リストへ戻る
7. [ ] 死亡推定時刻ON → 範囲内カードに赤バー表示
8. [ ] 書き出し → 全データ削除 → 取り込み → 完全復元されている
9. [ ] リロードしても状態（表示モード・トグル類・データ）が保持されている
10. [ ] コンソールにエラーが出ていない

---

## 7. 中断・撤退の判断基準

- 各フェーズでスモークテストに失敗し、**30分調査しても原因が特定できない場合**は `git checkout` で当該フェーズを破棄し、指示文を見直してから再実行する（ダラダラ直さない）。
- Phase 2〜3 の途中で長期中断する場合も問題ない（ラッパー方式なので新旧が混在しても動作は正しい）。ただし中断時は「どこまで切り替え済みか」をこのファイルの末尾にメモすること。

## 8. 進捗メモ（実施のたびに追記）

| 日付 | フェーズ | 結果 | メモ |
|---|---|---|---|
| | Phase 0 | 未着手 | |
| | Phase 1 | 未着手 | |
| | Phase 2 | 未着手 | |
| | Phase 3 | 未着手 | |
| | Phase 4 | 未着手（任意） | |
| | Phase 5 | 未着手 | |
