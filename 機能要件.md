# マダミス専用メモアプリ（マダミス時系列整理.html）機能要件書

本書は、単一HTMLファイル `マダミス時系列整理.html` の現在の挙動を**確実に再現するため**の機能要件を定義する。
色・寸法・余白などの具体値も、挙動再現に必要な情報として機能要件に含める。

- 「PC版」= `body.view-pc` が付いた状態、「スマホ版」= `body.view-mobile` が付いた状態を指す。
- 色は `#RRGGBB` 表記、寸法は CSS の px 値をそのまま記載する。
- 本書で「保存する」は、明示のない限り localStorage への保存を指す。

---

## 1. 全体・表示モード

### 1.1 基本構成
- 単一HTMLファイルで動作し、サーバー・外部ライブラリ・外部通信を一切使用しない。
- ページ全体の背景色は `#2c3e50`、文字色は `#fff`、`body` の padding は 10px。
- 各パネル（`.panel`）は背景 `#34495e`、padding 15px、角丸 8px、影 `0 3px 8px rgba(0,0,0,0.30)`。
- `.main-container` は最大幅 1400px・中央寄せ、パネル間 gap 15px。
- `html` に `-webkit-text-size-adjust: 100%; text-size-adjust: 100%;` を指定し、モバイルの自動文字拡大を無効化する。
- フォントスタック（body）: `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Hiragino Kaku Gothic ProN", "Hiragino Sans", "Yu Gothic UI", "Noto Sans JP", Meiryo, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`。`button/input/select/textarea` は `font-family: inherit` で同スタックを継承する。
- モノクロ記号ボタン（`.move-arrow-icon`, `.compare-move-btn`, `.char-action-icon`, `.session-data-toggle`）は記号フォント優先のスタック（`"Hiragino Sans", "Segoe UI Symbol", "Noto Sans Symbols", "Noto Sans Symbols 2", …`）を使う。
- カラー絵文字（✏️🚩👁️⬆️⬇️🗑️等）には異体字セレクタ FE0F、モノクロ記号（▶︎⬆︎⬇︎⬅︎➡︎↩︎）には FE0E を付ける。
- チェックボックスは全て 15px × 15px 固定、`margin: 0`、`accent-color: #4697DB`。
- タッチ端末（`@media (hover: none) and (pointer: coarse)`）では `body { cursor: pointer; }` を当て、余白タップでも document に click が届く（＝ポップアップの外側タップ閉じが効く）ようにする。

### 1.2 スマホ版 / PC版の切替
- 画面右上に「📱 スマホ版」「💻 PC版」のトグルボタンを表示する。選択中のボタンは背景 `#4697DB`・白文字、非選択は背景 `#34495e`・文字 `#cbd5e1`・枠 `#4a627a`。左ボタンは左側のみ角丸 6px、右ボタンは右側のみ角丸 6px で連結表示する。
- 初回（未保存）は画面幅 900px 未満ならスマホ版、以上ならPC版を初期値とする。選択は `madamisu_view` に保存し、次回以降復元する。
- **PC版の自動縮小（view-compact）**: PC版選択中でも、リスト/人物比較のモード切替ボタン（`#modeCompareBtn`）の文字が折り返す（ボタン高さ > 1行分×1.5 + 上下padding/border）と判定された場合、実効レイアウトをスマホ版（`view-mobile`）に切り替え、あわせて `view-compact` クラスを付ける。ユーザー選択（PC版）とトグルボタンの点灯状態は保持する。
  - `view-compact` 時は、`.main-container` は横並び（PC版の2カラム）を維持し、左カラム（キャラクター一覧・メモ）は `flex: 0 0 300px` のPC版レイアウトのまま、**時系列パネルの中身だけ**スマホ版の見た目になる。セッション名見出しもPC版の2段構成を維持する。
- ウィンドウリサイズ時は `requestAnimationFrame` でデバウンスし、**横幅が変わったときだけ**（高さのみの変化＝スマホのアドレスバー開閉などでは再計算しない）`applyView()` とカード右端揃えを再実行する。

#### PC版レイアウト
- `.main-container` を `flex-direction: row; align-items: flex-start` の2カラムにする。左カラム（`.side-column`：セッション名・キャラクター一覧・秘密のメモの縦積み）は `flex: 0 0 300px`、時系列パネルは `flex: 1; min-width: 0` で残り幅全体。
- トップバーは左詰め（`justify-content: flex-start; flex-wrap: nowrap`）にし、表示モード切替のみ `margin-left: auto` で右端に置く。展開したセッションデータ操作群は「▼ セッションデータ」の右横（`margin-left: 8px`、折返しなし）。

#### スマホ版レイアウト
- `.main-container` は縦積み。並び順は「左カラム（order:1）→時系列パネル（order:2）」。
- トップバーは `justify-content: space-between; flex-wrap: wrap`。展開したセッションデータ操作群は `flex-basis: 100%` で下段全幅に落ちる。
- 「⬇️ 取り込み」と「👤 全キャラクター削除」の間で強制改行する（`.data-actions-break` を `flex-basis:100%` で表示。PC版では 非表示で、代わりに `.data-actions-gap` の `margin-left: 12px` により通常 gap 8px と合わせて20px相当の間隔を空ける。スマホ版では `margin-left: 0`）。

---

## 2. データ保存（localStorage）

### 2.1 保存キー
すべて接頭辞 `madamisu_` を持つ以下のキーを使用する:
`events` / `characters` / `char_filter` / `memo` / `session_name` / `memo_concealed` / `death_from` / `death_to` / `death_enabled` / `view` / `timeline_mode` / `flag_filter` / `confirm_delete` / `show_concealed_compare` / `compare_order` / `compare_order_fixed` / `compare_zoom` / `session_data_expanded`（実キー名は `madamisu_events` 等）。

### 2.2 堅牢性
- 読み込みは JSON パース失敗時にフォールバック値（`[]` 等）で初期化し、例外を投げない。
- 書き込み失敗（容量超過・プライベートモード等）時は console.warn し、**セッション中1回だけ** `alert('保存に失敗しました（ブラウザの容量制限やプライベートモードの可能性があります）')` を表示する。

### 2.3 イベントデータ構造と後方互換
- イベント: `{ time, text, characters: string[], character（先頭1人・後方互換）, color, flag, endTime?, concealed?, colorHidden?, anchor? }`。
- 読み込み時、旧形式（`characters` 配列がない）データは `character` から `characters` 配列を生成して移行する。
- 人物名の参照は常に `eventCharNames(ev)`（配列がなければ `character` 単数から生成）を通す。

---

## 3. トップバー：セッションデータ

### 3.1 開閉トグル
- 「▶︎ セッションデータ」ボタン（背景 `#34495e`、文字 `#cbd5e1`、枠 `#4a627a`、角丸 6px、フォント 0.8rem 太字。hover で背景 `#405a70`・白文字）を押すと操作群を開閉する。開時は表示が「▼ セッションデータ」に変わり、`aria-expanded` も切り替える。開閉状態は `madamisu_session_data_expanded` に保存・復元する。

### 3.2 書き出し（エクスポート）
- 「⬆️ 書き出し」ボタン（緑系: 背景 `#14532d`、文字 `#bbf7d0`、枠 `#16a34a`。hover で `#166534`・白文字）。
- 全保存キーの生値を `{ app: 'madamisu', version: 1, exportedAt: ISO日時, data: {キー: 値} }` 形式のJSON（インデント2）にまとめ、Blob ダウンロードする。
- ファイル名は `{セッション名}_時系列データ_{YYYY-MM-DD}.json`。セッション名が空なら「マダミス」。`\ / : * ? " < > |` は `_` に置換する。

### 3.3 取り込み（インポート）
- 「⬇️ 取り込み」ボタンで非表示の `<input type="file" accept="application/json,.json">` を開く。選択後は input.value をクリアし、同じファイルを続けて選べるようにする。
- 検証: JSONパース可能かつ `app === 'madamisu'` かつ `data` がオブジェクトであること。既知キーが1つも含まれなければ「読み込めるデータが含まれていませんでした。」と表示して中止。
- 実行前に「現在の保存データを、選んだファイルの内容で置き換えます。…よろしいですか？」の confirm を出す。
- 既存データを退避してから全既知キーを削除→取り込み（文字列値のみ）。失敗時は退避データから復元し、エラーメッセージを表示する。成功時は `location.reload()`。

### 3.4 削除系（すべて赤系ボタン: 背景 `#7f1d1d`、文字 `#fecaca`、枠 `#b91c1c`。hover で `#991b1b`・白文字）
- 「👤 全キャラクター削除」: confirm 後、全イベントの `characters` を空・`character` を空にして保存し、`characters`/`char_filter` キーを削除、reload。**カード・メモ・設定は残る**。
- 「🧹 全タイムラインデータ削除」: confirm 後、`events`/`char_filter`/`flag_filter` キーを削除、reload。**キャラクター・メモは残る**。
- 「🗑️ 全データ削除」: confirm 後、全既知キーを削除、reload。

---

## 4. セッション名パネル

- 見出しは両モードとも「 マーダーミステリー『（セッション名）』」表記（`::before`/`::after` で `『』` を付ける）。「🎭 セッション名：」形式のラベルは常に非表示。
- 未入力時はプレースホルダー風表示（色 `#94a3b8`・非太字）。文言は **PC版:「クリックしてタイトルを入力」／スマホ版:「タップしてタイトルを入力」**。
- 表示テキストのクリック（またはタップ）で編集開始。値があるときのみ ✎（鉛筆）ボタンを表示し、押しても編集開始。編集中は ✎ が ✓ に変わる（クラス `editing`：フォント 1.25rem、位置 left 2px）。
- 編集確定は「✓ ボタン押下」または「**700ms 以内に Enter を2回**」。確定時に trim して `madamisu_session_name` に保存し表示に戻る。✎ ボタンは `onmousedown="event.preventDefault()"` でフォーカス移動を防ぐ。
- 編集中にページを閉じる・リロード・アプリ切替をしたとき（pagehide / visibilitychange: hidden）は、編集途中の値を trim して自動保存する（入力消失の防止。通常の確定操作は上記のまま）。
- PC版（および view-compact）ではラベル行と値行の2段構成（`flex-direction: column; gap: 4px`、ラベルに `padding-left: 0.5em`、パネルの padding-top 18px / padding-bottom 12px）。スマホ版は1行構成で `padding-bottom: 0`。

---

## 5. キャラクター一覧パネル

### 5.1 追加
- 名前入力欄＋色選択ボタン＋「追加」ボタンの1行（`flex-wrap: nowrap`、中央寄せ）。名前入力欄はPC版 `flex: 0 1 130px`、**スマホ版のみ `flex: 0 1 220px`** に広げる。
- 名前は trim し、空なら何もしない。**同名は登録不可**（`alert('そのキャラクター名は既に登録されています')`）。
- 名前入力欄で **Enter を素早く2回**（700ms 以内・長押しリピート除外）押しても「追加」ボタンと同じ処理が実行される。
- 追加後、名前欄をクリアする。選択色はリセットしない。

### 5.2 カラーパレット（共通仕様）
- 候補色は以下の**14色固定**:
  `#ef4444, #f97316, #f59e0b, #eab308, #84cc16, #22c55e, #14b8a6, #06b6d4, #3b82f6, #6366f1, #8b5cf6, #a855f7, #ec4899, #78716c`
- 初期選択色は `#3b82f6`。色選択ボタン（40×36px、角丸4px）の背景が現在の選択色になる。
- パレットはボタン下 6px・右寄せのポップアップ（背景 `#2c3e50`、枠 `#4a627a`、角丸6px、7列グリッド、gap 6px、z-index 10）。スウォッチは 24px の円で hover 時 1.15倍拡大、選択中は白枠＋ `#3b82f6` の外周リング。
- スウォッチクリックで選択・ポップアップを閉じる。**ポップアップ外クリックでも閉じる**。

### 5.3 一覧の各行（`.char-item`: gap 10px、padding 8px、下線 `#4a627a`）
- 表示: 色ドット（18px円）＋名前＋操作ボタン群。
- **通常時のボタン**（各 25×21px）:
  - `✕`（削除・背景 `#fee2e2`・文字 `#ef4444`）: confirm「キャラクター「名前」を削除しますか？…」後に削除。削除時は各カードの登場人物から当該名を外す（**カード自体は残る**）。追加フォームの選択・絞り込みからも外す。
  - `✏️`（名前編集）: インライン編集モードへ。
  - `🔓`（ロック）: `locked: true` を保存。**ロック中は ✏️ と 🔓 が消え、✕ が 🔐（背景 `#fef9c3`）のロック解除ボタンに変わる**。🔐 押下でロック解除。ロック中は編集・削除不可。
- **名前編集モード**: テキスト入力＋🎨（色選び直しパレット）＋✓（保存。リスト/人物比較ビューの✓ボタンと同じ配色：背景 `#dcfce7`・枠 `#86efac`・文字 `#16a34a`。hover時は同背景のまま `brightness(0.95)`。ボタン・チェック記号のサイズは通常時ボタンと同じ）＋↩︎（キャンセル）。編集開始時に入力へフォーカスし全選択。
  - 保存時: 空または変更なしならキャンセル扱い。他と重複なら alert。確定したら**全イベントの登場人物名・追加フォーム選択・絞り込み・比較ビュー列順（compareOrder）内の旧名を新名へ一括置換**して保存・再描画する。
- **色変更**: 🎨 で当該キャラ専用パレットを開き、選択即反映（全カードのバッジ・カード色も再描画で追従）。他のパレットが開いていれば閉じる。

### 5.4 色の安全性
- インラインstyle・動的CSSに使う色は `#RRGGBB` 形式のみ許可し、不正値は `#94a3b8` に置換する（`safeColor`）。未登録・削除済みキャラの色は `#94a3b8`。

---

## 6. 秘密のメモパネル

- 見出し「📝 秘密のメモ」＋右端に 👁️ ボタン。
- テキストエリア（最小高さ 260px、白背景・文字 `#333`、行高 1.6、縦リサイズ可）。プレースホルダー「自分の推理や、嘘をつくポイントなどを自由に書き込めます...（自動保存されます）」。
- 入力は **300ms デバウンスで自動保存**する。さらに、タブを閉じる・リロード・アプリ切替の直前（`pagehide` / `visibilitychange`のhidden時）にはデバウンス待ちを即座に保存し、直前の入力が失われないようにする。
- 👁️ でメモ全体の表示/非表示を切替（状態を保存・復元）。非表示中はテキストエリアを隠し「（非表示中）」ラベルを表示、👁️ ボタンは控えめな色調（背景 `#cbd5e1`、文字 `#475569`、grayscale 0.65、opacity 0.75）になる。

---

## 7. 時系列整理：追加フォーム

### 7.1 構成
時間入力（幅80px・中央寄せ）→「〜」（期間モード時のみ表示）→終了時間入力（幅80px・**期間モード時のみ表示**）→「⏱ 期間」トグル→「人物 ▾」ボタン→できごと入力（`flex-grow: 4`）→ 🚩 トグル→「追加」ボタン（背景 `#4697DB`、hover `#397bc0`）。

### 7.2 時間入力の整形（追加・編集の全時間欄共通）
- **input 中**: 全角英数記号を半角化。半角数字4桁が揃った瞬間に `HH:MM` へ自動整形（例: `1030`→`10:30`）。カーソル位置は「数字何個目の直後か」を基準に維持する。
- **フォーカスアウト時**: `normalizeTime` で整形。規則:
  - 数字・空白・区切り記号以外の文字が混ざる文字列（「夕方」「不明」「深夜2:30頃」等）は、全角→半角化のみ行い**一切加工せずそのまま**受け付ける（自由記述可。入力した文字を消さない）。
  - 全角→半角、区切り記号 `: ： ; ；` を `:` に統一、連続 `:` は1つに。
  - `:` を含む場合は「時:分」として、分を2桁に0埋め（`20:5`→`20:05`）、**時は先頭ゼロを除去**（`09:05`→`9:05`。表記ゆらぎで同時刻が別扱いになるのを防ぐ）。分が空なら時のみ返す。時が空で分があれば `0:MM`。
  - 数字のみ4桁→`H:MM`（時の先頭ゼロは除去。`0905`→`9:05`）、3桁→`H:MM`。
- 対象欄: `#timeInput` `#endTimeInput` `.edit-time` `.edit-endtime` `.edit-time-inline`。

### 7.3 追加（addEvent）
- **できごとは必須**。空なら何もしない（時間・人物は空でもよい）。
- できごと入力欄（`#eventInput`）で **Enter を素早く2回**（700ms 以内・長押しリピート除外）押しても「追加」ボタンと同じ処理が実行される。
- 保存内容: `time`（normalize済み）、`text`、`characters`（選択中の人物のコピー）、`character`（先頭1人）、`color`（先頭人物の色、人物なしは `#fafafa`）、`flag`（🚩トグル状態）。
- 期間モードONのとき、終了時間が**有効な時刻（HH:MM とパースできる）場合のみ** `endTime` を保存する。無効・空なら endTime なしの通常イベントになる。
- 追加後の後処理（**毎回必ず実行**）:
  1. 時間欄・できごと欄をクリア。
  2. 🚩 フラグを OFF に戻す（ボタンの点灯も解除）。
  3. `resetAddEventForm()` を呼び、**「期間」モードを OFF**（`range-mode` クラス・トグル点灯を解除し終了時間欄をクリア）、**人物選択を全解除**（`newEventChars = []`、ボタン表示を「人物 ▾」へ、ポップアップのチェック再描画、開いているポップアップを閉じる）。
- 追加後は時間順にソートして保存・再描画する。

### 7.4 「⏱ 期間」トグル
- 押すたびに期間モードをON/OFF。ON時はボタンが点灯（背景 `#dbeafe`、枠 `#3b82f6`、文字 `#1e40af`・太字）し、「〜」と終了時間欄が現れる。OFF に戻すと終了時間欄をクリアする。

### 7.5 「人物 ▾」複数選択ポップアップ
- 押すと下方向にチェックボックス付きの人物リストを開く（背景 `#2c3e50`、枠 `#4a627a`、min-width 170px、max-height 260px 縦スクロール、z-index 30）。行は「チェックボックス＋14px色ドット＋名前」で hover 背景 `#3b4b5e`。
- キャラ未登録時は「先にキャラクターを登録してください」と表示。
- チェックのオン/オフで選択を切替。ボタンの表示は **0人:「人物 ▾」／1〜2人: 名前を「・」区切り＋「 ▾」／3人以上:「N人選択 ▾」**。ボタン最大幅 220px（あふれは省略記号）。
- 外側クリックで閉じる。開くとき、他の人物ポップアップは全て閉じる。

### 7.6 🚩 トグル
- OFF時はグレースケール＋opacity 0.55。ON時はフルカラー・背景 `#fefce8`・枠 `#fde047`。次に追加する1件にフラグを付ける（追加後は必ずOFFに戻る）。

---

## 8. 死亡推定時刻

- 時系列パネル見出し行の右側に「⚰️ 死亡推定時刻」＋開始/終了のプルダウン（時: `--`,`00`〜`23`／分: `--`,`00`,`15`,`30`,`45`）＋ON/OFFボタンを表示する。
- 「時」を選択したとき「分」が未選択（`--`）なら、分を自動で `00` にしてから保存する（時だけ選んで未設定のままになる事故を防ぐ）。
- プルダウン変更時、時・分の両方が選択されている側だけ `HH:MM` として保存する（片方 `--` なら空）。値は保存・復元する。
- ON/OFFボタン: OFF時は背景 `#64748b`・表示「OFF」、ON時は背景 `#BF1D37`・表示「ON」。丸型（border-radius 999px）、min-width 54px。状態を保存・復元する。
- **範囲判定**: ONかつ開始・終了とも有効な時刻のとき、開始≦終了なら「開始≦t≦終了」、**開始＞終了なら日跨ぎ範囲（t≧開始 または t≦終了）**として扱う。時刻でないカード（不詳等）は対象外。
- 開始＞終了のときは警告帯「⚠️ 開始が終了より後です。日付を跨ぐ範囲（例: 23:00〜翌1:00）として扱います。」（背景 `#fef3c7`、文字 `#92400e`、枠 `#fcd34d`）を表示する。
- **リストビューでの表示**: 範囲内のカードは `::before` 疑似要素で、カード**左外側 12px の位置**（left: -12px）に幅 5px・角丸 3px の縦バー（`#BF1D37`）を表示する。連続する範囲内カードはバーを上へ 9px 伸ばし（top: -9px、上側角丸なし）、カード間余白 8px をまたいで**1本の連続バー**に見せる。フラグの金色帯（カード内側）とは重ならない。
- 期間イベントの終了カードは `endTime` で範囲判定する。
- **人物比較ビューでの表示**: 範囲内の時刻行の時間ラベルを明るい赤 `#ef4444` にする。

---

## 9. リストビュー（カード表示）

### 9.1 表示行の組み立てルール
- 表示対象: フラグ絞り込み（§11.2）と人物絞り込み（§11.1）を通過したイベント。
- **期間イベント判定（isRangeEvent）**: `endTime` があり、かつ開始・終了の**両方**が有効な時刻の場合のみ期間イベント。
- 期間イベントは、その開始〜終了の**間（両端は含まない）**に他の表示対象イベントの時刻マーカー（点イベントの時刻、他の期間の開始/終了時刻）が存在する場合のみ「開始行」＋「終了行」の2枚に分割する（→コネクター描画、§9.6）。**間に何もなければ1枚のカード（range-single）**として表示する。
- 並び順: 実効時刻の昇順で安定ソート。実効時刻は「有効な時刻ならその分数」「不詳は `anchor`（手動位置）があればその値、なければ 100000（末尾側）」。開始行は開始時刻、終了行は終了時刻の位置に並ぶ。
- **有効な時刻の定義**: 文字列全体が「時:分」（時は1〜2桁、分は2桁かつ59以下）として丸ごと読めること。前後に他の文字が混ざる自由記述（例「会議は13:00頃?」）や分が60以上（例「9:99」）は時刻とみなさず、不詳（末尾側）扱いになる。

### 9.2 カードの基本外観
- カード（`.timeline-item`）: 背景 `#f0f2f5`、文字 `#333`、padding `10px 10px 10px 16px`、下余白 8px、角丸 6px、gap 8px、`position: relative`。
- **時間バッジ**（`.time-badge`）: 幅 60px 固定（`flex: 0 0 60px`）、背景 `#e2e8f0`、太字 0.9rem、角丸 20px、中央寄せ、文字 `#333`。時刻なしは「**不詳**」と表示する。
- **人物バッジ**（`.char-badge`）: 幅 152px 固定（`flex: 0 0 152px`）、角丸 20px、太字 0.9rem、padding `4px 10px`。背景はその人物の色、文字色は知覚輝度（`0.299R+0.587G+0.114B > 150` なら `#333`、以下なら `#fff`）で自動決定。名前があふれる場合は省略記号。
- **できごと本文**（`.event-text`）: `flex: 1 1 200px`、0.95rem、行高 1.4、`word-break: break-all`。
- **カード全体の色**:
  - 人物1人: その人物の色でカード全体を塗る（キャラindexごとの動的CSS `char-idx-{i}` を `<style id="cardColorsStyle">` に生成）。
  - 人物2人以上: 背景 `#C3CDDB`（`multi-character`）。
  - 人物なし: 既定の `#f0f2f5`（`no-character` クラス。枠線色は複数人カードと同じ）。
- **操作ボタンの枠線色**: カード色から算出した「彩度×0.85・明度を色相帯ごとに調整（赤0.70／桃0.72／橙0.68／黄緑0.64／緑0.61／青緑0.55／水色0.61／藍〜紫0.74／低彩度0.58／その他0.70）」の色を CSS変数 `--card-btn-border` として使う。色隠し中カードは `#a1a1a1`。

### 9.3 複数人物バッジの表示
- 人物バッジは縦並び（gap 4px）。**3人以上のときは2人まで表示**し、「＋他を表示」ボタン（破線枠・角丸20px）で残りを展開（展開後は「－ 閉じる」）。
- 人物なしのカードには幅 152px の**空スペーサー**を置き、列位置を揃える。
- **PC版のみ**: 複数人カード（multi-character）はバッジを**横並び・折返し**（gap 6px）で**全員表示**し、「＋他を表示」は出さない。バッジは内容と同じ幅で折り返す。時間バッジはカード縦中央に揃える。
- **スマホ版**: 複数人カードのバッジは横並び・折返しで全員表示（PC版同様）。時間バッジは**一番上の行に上寄せ**（`align-self: flex-start`）。

### 9.4 カードの操作ボタン（`.controls`、gap 4px）
右端に次の順で並ぶ。共通スタイル: 背景 `rgba(255,255,255,0.12)`、枠 `--card-btn-border`（既定 `#a1a1a1`）、角丸4px、padding `4px 7px`、文字 `#555`、hover 背景 `#e2e8f0`。
1. **⬆︎ / ⬇︎（移動）**: 不詳カード、または**同じ時刻のカードが複数あるカード**にのみ表示（§9.5）。文字色 `#3B3B3B`、矢印は 1px 下げ配置。
2. **✏️（編集）**: フル編集モードへ（§9.7）。
3. **👥（登場人物）**: カード用の人物選択ポップアップ（`.char-assign-popup`、右寄せに開く）を開閉。チェックのオン/オフで人物を追加/削除し、即保存・再描画。再描画後もポップアップは開いたまま維持し連続操作できる。ポップアップを開いている間、そのカードは `z-index: 40` に持ち上げ、下のカードのボタンに覆われないようにする。人物変更時はカードの `color` を先頭人物の色（なしは `#fafafa`）に更新する。
4. **🚩（フラグ）**: ON/OFF切替。OFF時は絵文字のみグレースケール（枠線色は保持）、ON時は背景 `#F9FFBF`・枠 `#fde047`。フラグONのカードは左端に**内側**の金色帯 `box-shadow: inset 5px 0 0 #eaff00` を表示する。
5. **👁️（内容の非表示）**: ONで concealed 状態（§9.8）。
6. **🎨（色を隠す）**: **非表示（concealed）中のカードにのみ表示**。ONでカード背景を `#e5e7eb` に、バッジも `background:#e5e7eb; color:#9ca3af` にグレーアウト。concealed を解除すると colorHidden も自動解除される。
7. **✕（削除）**: 背景 `#fee2e2`・文字 `#ef4444`・枠 `#fca5a5`。「カード削除時に確認画面を表示」がONなら confirm「このカードを削除しますか？」を出す（OFFなら即削除）。
- フラグ・非表示・色隠しの切替は、**リストビューの期間イベント以外は全再描画せず該当カードだけを部分更新**（refreshCard）する。期間イベントと人物比較ビューは常に全再描画する。

### 9.5 移動（⬆︎ / ⬇︎）
- **不詳カード**: 移動先の上下の行の実効時刻の中間値を `anchor` に設定して並べ替える（同値の場合は ±0.5、端の場合は ±60 のダミー値で補う）。時刻ありカードの間を含む任意の位置へ移動できる。表示は「不詳」のまま。
- **同じ時刻の複数カード**: 隣の行が同じ時刻のカードのときだけ、`events` 配列内で**要素を直接入れ替える**（anchor は使わない）。隣が違う時刻・終了行の場合は何もしない。
- 隣の判定は**絞り込みを無視した全行**（buildSortedRows(false)）で行う。
- 後から時刻を入力・保存した場合、`anchor` は削除される。

### 9.6 期間イベントとコネクター（コの字）
- **1枚カード（range-single）**: 時間バッジ内に開始・終了を `<br>` 2行表示（padding 6px 8px、編集可能時 line-height 1.62）。右端に、その期間の縦バー（後述の conn-bar）が cap-top＋cap-bottom 付きで付く。
- **分割時（開始カード＋終了カード）**:
  - 終了カード（`range-end-card`）は時間バッジに**終了時刻のみ**表示（バッジ文字色 `#7c8799`）、本文・操作ボタンなし。カード背景は期間色（`--conn-color`）で塗られ、**下の横棒**の役割を持つ。死亡推定時刻の判定は endTime で行う。
  - **縦バンド（`.conn-bar`）**: 幅 **25px** の絶対配置要素。各カードの右側に置かれ、`top: 0; bottom: -8px` でカード間余白 8px をまたいで連続する。期間の最上行では上右角丸 6px（cap-top）、最下行では `bottom: 0`＋下右角丸 6px（cap-bottom）で閉じる。
  - **期間色（rangeColor）**: 色隠し中は `#e5e7eb`。人物2人以上は `#C3CDDB`（複数人カードと同色）。1人はその人物色。人物なしは `#94a3b8`。開始・終了カード（1人の場合 `char-idx-{i}` で同色に塗る）と縦バンドが同色でつながり「コ」の字に見える。
  - **入れ子（ネスト）**: 重なる期間は深さ（depth）を割り当てる。並び順は top 昇順→bot 降順→index 昇順で、既に置かれた期間と行が重なる場合は空いている最小の depth に入れる。depth 0 が最も右（外側）、深いほど左（内側）。バンド間隔は **31px（線幅25px＋余白6px）**、通常カード内容とバンドの間は **7px**。
  - **カード背景の右マージン（コの字保証・最重要）**:
    - 自分の腕（自分の期間の端のバンド）を持たない点カード等は、全バンドを避けて `(最大depth+1)×31+7` px 右マージンを取る。
    - 自分の腕を持つ開始/終了カードは、自分より**外側（浅いdepth）**のバンドだけを避ける。外側バンドがなければ右マージンは `自分のdepth×31` px（＝**背景の右端を自分の腕の右端に揃える**）。
    - **安全策**: 上記計算がどう変わっても、腕を持つカードの右マージンは必ず `自分のdepth×31` px 以上に clamp する。**背景が縦バンドより右へはみ出すと「コ」の字が「ユ」の字に見えるため、これを恒久的に禁止する**。
    - カード内容の右パディングは全バンドを避ける量（最小 7px）。
    - 各バンドの水平位置は `depth×31 − 右マージン` を right 値として全カードで同じ絶対位置に揃える。
- 期間の間に他のカードが挿さった/抜けた瞬間に、1枚カード⇔分割表示が自動で切り替わる。
- 開始＞終了（日跨ぎ）の期間も、物理的な上下（min/max）で正しく描画する。

### 9.7 編集
- **フル編集（✏️）**: カード内が「時間（幅64px）〜終了時間（幅64px・placeholder「任意」）・内容」の入力群＋✓✕ボタン（各30×30px。✓は緑系 背景 `#dcfce7`・枠 `#86efac`・文字 `#16a34a`、チェック記号は1.25倍・上下オフセットなし）に置き換わる。入力欄の高さは36px固定。
  - 保存: 内容が空なら `alert('内容を入力してください')` で拒否。時間・終了時間は normalize。**終了時間が有効な時刻なら期間イベント化、空・無効なら単一時刻に戻す（`endTime` キー自体をデータから削除する）**。時刻が入ったら `anchor` を削除。ソートして保存・再描画。この保存処理は比較ビューの編集と共通（`applyEventEdit`）。
  - 取消は ✕ ボタンのみ（リストのフル編集に Escape キーの取消は**ない**。Escape 取消があるのは比較ビューの編集と時間だけ編集）。**時間・終了時間・内容のいずれの欄でも Enter を素早く2回**（長押しリピート除外。1回目から**700ms以内**に2回目。セッション名編集と同じ猶予時間）押すと保存。
  - フル編集を開始すると時間だけ編集は解除される。編集中は一度に1カードのみ。
  - PC版では ✓✕ 群はカード縦中央揃え。
- **時間だけ編集（時間バッジクリック）**: 点イベント・分割開始カードの時間バッジは `cursor: pointer`、hover で背景 `#cbd5e1`。クリックでバッジ内がインライン入力（白背景、青枠 `#3b82f6`、角丸16px、中央寄せ）に変わり、フォーカス＋全選択。Enter または **blur で保存**、Escape でキャンセル。保存時は normalize・anchor削除・ソート。
  - **range-single（1枚期間カード）の時間バッジは、クリックで時間だけ編集ではなくフル編集（✏️と同じ）を開く**。
  - 終了カードの時間バッジは編集不可。

### 9.8 非表示（concealed）
- 👁️ ONのカードは、内容・時間・バッジ・✏️👥⬆︎⬇︎✕を隠し、「（内容を非表示中）」（0.85rem・イタリック）と 👁️🚩🎨 だけを表示する。
- 非表示中の 👁️ ボタンは控えめな色（背景 `#cbd5e1`、文字 `#475569`、grayscale 0.65、opacity 0.75）。🎨 ON時も同色調。色隠し中のラベル文字色は `#333`。

### 9.9 操作ボタンの右端揃え（alignControlsToShortestCard）
- 描画後、（終了カード・編集中を除く）全カードの操作ボタン群の右端を、**最も左にある（＝カード幅が最短の）ボタン群の右端**に `transform: translateX()` で揃える。
- PC版では、ずらした結果ボタンが内容に重なる場合、カード内側に paddingRight を追加して回避する。
- 計測は「読み取り一括→書き込み一括」の2段階で行い、レイアウトスラッシングを避ける。
- ビュー切替・リサイズ（横幅変化時）・再描画のたびに再実行する。

### 9.10 スマホ版のカードレイアウト
- カードは `flex-wrap: wrap`。**非表示中でなく編集中でもないカード**は、`timeline-item-inner` と `event-body` を `display: contents` で解除し、カード直下の flex に「時間バッジ→人物バッジ→本文（order:4・全幅）→操作ボタン（order:5・全幅・右寄せ）」を並べる（本文と操作ボタンは各々次の行に落ちる）。
- `display: contents` により inner の z-index が効かないため、時間バッジ・人物バッジ・スペーサー・操作ボタン・本文へ個別に `position: relative; z-index: 1` を与え、コネクターバンドより前面にする。
- **⬆︎⬇︎の退避**: カードが狭く、✏️ボタンが時間バッジの真下（水平方向に重なる位置）に来たカードには `unknown-moves-relocated` を付け、⬆︎⬇︎を絶対配置で ✕ の右隣（左100%＋4px、⬇︎はさらにその右）へ移す。判定は毎回リセットしてから通常配置で計測するので、画面が広がれば元に戻る。
- 人物バッジは `flex: 0 1 auto; max-width: 100%`。

### 9.11 PC版のカードレイアウト
- 時間バッジはカード縦中央揃え、本文（event-text）は `flex: 1 1 100%; margin-left: 10px` で2行目に全幅で回り込み、行間 row-gap 11px。操作ボタン群はカード下端揃え（編集中は中央揃え）。

---

## 10. カード色の動的CSS

- キャラクターの数・色が変わるたびに `<style id="cardColorsStyle">` へ以下を生成する:
  - `.timeline-item.char-idx-{i} { background: 色; --card-btn-border: 枠線色; }`（キャラ名でなく**インデックス由来のクラス名**を使い、CSSインジェクションを防ぐ）
  - `.timeline-item.multi-character { --card-btn-border: mutedBorderColor(#C3CDDB); }`
  - `.timeline-item.no-character { --card-btn-border: mutedBorderColor(#C3CDDB); }`
  - `.timeline-item.color-hidden { --card-btn-border: #a1a1a1; }`

---

## 11. 絞り込みフィルタ

### 11.1 人物絞り込み
- キャラが1人以上いるとき、時系列パネル上部に「表示対象絞り込み」ラベル＋チップ群を表示する。チップは丸型（border-radius 999px、0.8rem）。
- 「全員」チップ（active時 背景 `#64748b`）: 絞り込み解除。人物未選択のとき active。
- 人物チップ（12px色ドット＋名前。active時 背景 `#3b82f6`・白文字）: **複数選択可**のトグル。選択中の人物が**1人でも含まれる**カードを表示する。
- 選択は `madamisu_char_filter` に保存・復元。削除済みキャラは自動的に外れる。

### 11.2 フラグ絞り込み
- 「🚩 フラグつき」チップ（active時 背景 `#eaff00`・文字 `#3a3a00`）／「フラグなし」チップ（active時 背景 `#94a3b8`・文字 `#1e293b`）。
- 状態は `all / flag / noflag` の3値。同じチップを再度押すと `all` に戻る。人物絞り込みと**併用可**。
- `madamisu_flag_filter` に保存。旧形式（`true`/`false`）からは `true`→`flag` に移行する。
- 人物比較ビューにも適用される。

---

## 12. リスト / 人物比較の切替

- 「📋 リスト」「📊 人物比較」の連結トグル（active時 背景 `#4697DB`）。モードは `madamisu_timeline_mode` に保存・復元。ボタン文字の折返しは「絵文字の後」「人物/比較の間」のみ許可（各語 nowrap）。
- 切替時は `applyView()` を再実行する（バー幅が変わるため自動縮小の再判定）。
- 同じ行に以下のチェック/操作を表示:
  - 「カード削除時に確認画面を表示」（常時表示。既定 ON。`madamisu_confirm_delete` が `'false'` のときだけ OFF）。
  - **比較モード中のみ表示**: 「非表示設定中アイテムを不可視化」「人物の並びを固定する」「表示倍率（−／%表示／＋／リセット）」。表示倍率グループは「人物の並びを固定する」から `margin-left: 22px` 離す。
- **スマホ版**では、これらのチェック/操作は各々 `flex-basis: 100%` で独立した行に落ち、上との間隔 8px。

---

## 13. 人物比較ビュー

### 13.1 列（人物）の決定と並び
- 列は「人物絞り込みで選択中の人物」（未選択なら全キャラ）。
- 列順は比較ビュー専用の並び順 `compareOrder`（`madamisu_compare_order` に保存）。キャラ一覧の順序とは独立。未登録の名前は除去し、`compareOrder` にないキャラは characters 順で末尾に補う。
- ヘッダーの各人物名の左右に ⬅︎ ➡︎ ボタンを置き、隣の列と入れ替える。**端の列の外側方向、および「人物の並びを固定する」ON中は disabled**（opacity 0.2）。
- 「人物の並びを固定する」: ON にした時点の並びを確定保存。ON中は矢印での並べ替え不可。OFF に戻しても並びは維持され、矢印が再び使える。キャラ改名時は compareOrder 内の名前も追従する。
- 列が0のとき: キャラ未登録なら「先にキャラクターを登録すると、…」、登録済みなら「上の「表示対象絞り込み」で比較したい人物を選ぶと、…」の案内文を表示する。

### 13.2 テーブル構造
- 横スクロール可能なラッパー（`.compare-wrap`、角丸6px）内の `<table>`。セル境界は `#4a627a` の1px線、四隅はカードと同じ角丸6px。
- ヘッダー行（人物名）: `position: sticky; top: 0`（z-index 2）。背景はその人物の色、文字色は輝度で自動。laneCount 分の colspan。
- 時間列（左端）: `position: sticky; left: 0`（z-index 1、ヘッダー左上は 3）。背景 `#2c3e50`、太字・中央寄せ。
- 対象イベント: フラグ絞り込みを通過し、（「非表示設定中アイテムを不可視化」ON時は concealed でない）かつ列の人物を1人以上含むもの。
- **時間行**: 対象イベントの時刻（期間は開始も終了も）を重複排除して昇順に並べる。
- **時間不詳行**: 時刻なしカードのうち `anchor` を持ち、かつ最後の時刻以前を指すものは、その位置に「時間不詳」行（文字 `#cbd5e1`・イタリック・0.85em）として時間軸に差し込む。同じ sortT なら時刻行が先。
- **時間なし行**: それ以外の時刻なしカードは最下段の「時間なし」行に、人物列ごとにまとめて表示する（laneCount 分の colspan）。

### 13.3 レーン（サブ列）割り当て
- 各人物列内で、イベントを重なりのないレーンに配置する。**期間イベントのグループを左側**、**通常カード＋時間不詳カードのグループを右側**に固定する（グループごとに開始レーンを分ける）。同一グループ内で行範囲が重なる場合、先に登録されたものが左レーン。
- 期間イベントは開始行〜終了行を `rowspan` で結合したセル（`compare-cell-range`）に、セル内側 4px の余白を空けた角丸6pxの塗りブロック（`.compare-range-fill`、絶対配置 inset 4px、overflow auto）で表示する。日跨ぎ期間は上下を正規化して全行を覆う。
- 空のセルも `<td class="compare-cell compare-lane">` として必ず出力する。
- セル最小幅: 通常レーン **178.66px**、時間不詳カード（⬆⬇付き＝6アイコン）を含むレーンおよび時間なしカードのある列 **232px**。

### 13.4 比較カードの表示
- カードは角丸6px・padding `5px 8px`・0.9rem。背景は**その列の人物の色**（複数人イベントでも列ごとの人物色）、文字色は輝度で自動、ボタン枠線は mutedBorderColor。
- フラグONは `inset 4px 0 0 #eaff00`。concealed のカードは本文の代わりに「（非表示中）」を表示し、👁️ボタンは控えめ色調（リストと同じ）。
- 操作ボタン（右寄せ・カード下部）: ✏️編集 → （**時刻なしカード＝時間不詳行・時間なし行のカードのみ** ⬆︎⬇︎）→ 🚩 → 👁️ → ✕。時刻ありのカード（点・期間）には ⬆︎⬇︎ は出ない。挙動・見た目のON/OFF表現はリストビューと同一。🎨（色隠し）ボタンは**比較ビューにはない**。
- 複数人イベントは、含まれる各人物の列すべてに同じカードとして表示される。

### 13.5 比較ビューのインライン編集
- ✏️ でカード内が編集ビューに変わる: できごと textarea（全幅、最小高さ3.4em、縦リサイズ可）→時間入力（幅92px）→終了時間入力（幅92px、placeholder「任意」）＋✓✕（右寄せ・終了欄と同じ行、下端揃え）。開いたら textarea にフォーカス＋全選択。
- 保存/取消/バリデーション: リストのフル編集と**共通の保存処理（`applyEventEdit`）**を使う（内容必須、終了時間の有効性で期間化/解除（解除時は `endTime` キーを削除）、anchor削除、ソート）。**Enter を素早く2回（1回目から700ms以内）で保存・Escape で取消**（textarea・時間・終了時間の全欄で有効。長押しリピート除外）。
- すでに編集中のカードで ✏️ を再度押しても何もしない。

### 13.6 表示倍率
- CSS `zoom` を `.compare-wrap` に適用する。範囲 **0.1〜2.0、刻み 0.1**、既定 1.0。−／＋で1段階ずつ、リセットで 1.0。ラベルは「N%」表示（幅40px中央）。`madamisu_compare_zoom` に保存・復元。他画面には影響しない。

---

## 14. ヘルプモーダル

- 時系列見出し横の「？」ボタン（1.1rem の円形）で開閉する。
- 全画面オーバーレイ（背景 `rgba(0,0,0,0.5)`、z-index 100、上寄せ padding 40px 16px、縦スクロール可）＋白背景モーダル（最大幅 640px、角丸10px、行高1.7）。右上の ✕ ボタン、または**モーダル外側（オーバーレイ自体）のクリック**で閉じる。
- 内容は使い方チュートリアル（基本の登録・カードボタン・期間・死亡推定時刻・複数人物と絞り込み・リスト/比較・空欄の扱い・セッション名/メモ・セッションデータ・表示設定）。

---

## 15. ポップアップ共通の閉じる挙動

document への click で、以下を**外側クリック時に閉じる**:
- 追加フォームのカラーパレット（ボタン自身のクリックは除外）
- キャラ行の色選び直しパレット
- 追加フォームの人物選択ポップアップ／カードの人物ポップアップ（閉じる際、カードの `char-popup-open`＝z-index持ち上げも解除）

---

## 16. セキュリティ・入力の安全性（挙動再現に必要な範囲）

- ユーザー入力をHTMLへ差し込む箇所は全て `escapeHtml`（`& < > " '` を実体参照化）を通す。
- 色値は `safeColor` で `#RRGGBB` のみ許可。
- 動的CSSのセレクタはキャラ名でなくインデックス（`char-idx-{i}`）を使う。
