# マダミス時系列整理アプリ 機能要件

マーダーミステリー（マダミス）用の、時系列とキャラクター情報を整理するための単一 HTML アプリ。
CSS と Vanilla JavaScript のみで構成され、すべてのデータはブラウザーの `localStorage` に自動保存される。

---

## 1. 全体構成

- **単一 HTML ファイル**：外部依存なし（CSS・JS を内包）。
- **データ永続化**：`localStorage` に自動保存。保存キーは `KEYS` オブジェクトに集約。
- **プライベートモード等での保護**：保存・読込は例外を握りつぶすヘルパー経由で行い、失敗してもアプリが停止しない。
- **旧ブラウザ検出**：モダン構文（オプショナルチェーン等）に非対応のブラウザ（iOS 13.4 未満の Safari 等）では、白画面のまま停止せず「新しいブラウザでご利用ください」という案内文に画面を差し替える。
- **レイアウト**：
  - 左列：セッション名 → キャラクター一覧 → 秘密のメモ（縦積み）
  - 中央：時系列パネル

---

## 2. セッション名

- 左列最上部に **「マーダーミステリー『…』」** 形式でセッション（作品）名を表示。
- **編集**：タイトル部分をクリック（タップ）するか、✎ ボタンでインライン編集。編集中は ✎ ボタンが ✓ に変わる。
- **確定**：✓ ボタン、または Enter を 2 回（短時間内）押すと保存。
- 未入力時は表示モードに応じて「クリック／タップしてタイトルを入力」というプレースホルダー風の案内を表示し、✎ ボタンは非表示にする。
- セッション名はデータ書き出し時のファイル名にも利用される。

---

## 3. セッションデータ（書き出し・取り込み・削除）

- 画面上部の **「▶︎/▼ セッションデータ」ボタン**で、データ操作ボタン群の表示・非表示を切り替え（開閉状態は保存）。

### データのバックアップ

- **⬆️ 書き出し**：すべての保存データを JSON ファイルに書き出す（バックアップ・別端末への移動・共有用）。
  - ファイル名は `セッション名_時系列データ_YYYY-MM-DD.json`（セッション名未入力時は「マダミス」）。
  - セッション名に含まれるファイル名に使えない文字（`\ / : * ? " < > |`）は `_` に置換する。
  - JSON には `app`・`version`・`exportedAt` と全保存値を含む。
- **⬇️ 取り込み**：書き出した JSON を読み込み、現在のデータを置き換える（確認ダイアログあり、完了後にリロード）。
  - 本アプリの書き出しファイル（`app: 'madamisu'`）以外や不正な JSON はエラーとして拒否。
  - 既知のキー（`KEYS`）に含まれる項目のみ取り込む。イベント・キャラクターは配列型であることを検証し、型が異なる場合は取り込まない。
  - 取り込みの途中で失敗した場合は、取り込み前のデータへの復元を試みる。

### データ削除

- **👤 全キャラクター削除**：キャラクターとカード上の登場人物のみ削除。メモ・カード・設定は残す（確認ダイアログあり）。
- **🧹 全タイムラインデータ削除**：時系列カードのみ削除（確認ダイアログあり）。
- **🗑 全データ削除**：キャラクター・メモ・時系列・各種設定をすべて削除（確認ダイアログあり、実行後にページを再読み込み）。
- 削除系はいずれも元に戻せない操作のため、実行前に確認する。

---

## 4. 表示モード（PC版 / スマホ版）

- 画面上部で **「📱 スマホ版」／「💻 PC版」** を切り替え可能。
- 初回は画面幅（900px 未満でスマホ版）で自動判定。選択内容は保存される。
- **PC版**：
  - 左列とタイムラインを横並び表示。
  - カード内は時間を縦中央、人物名の下に本文を配置。
  - 操作アイコン群はカード内容の**最下段**に揃える。
  - 折り返した本文と操作アイコンが重ならないよう、本文右側に余白を確保。
- **スマホ版**：
  - 各パネルを縦積み表示（左列 → タイムラインの順）。
  - カード内で時間・名前・本文・操作アイコンを段組み表示。
  - カード編集中の✓（保存）・↩︎（取消）ボタンは、カードの**右下**に寄せて表示（他の操作ボタン行と右端を揃える）。
- **PC版の自動縮小（view-compact）**：PC版を選んでいても画面幅が狭くモード切替ボタンの文字が折り返す場合は、時系列パネルの中身だけをスマホ表示に自動切替する（左列の2カラムレイアウトはPC版のまま、ユーザーの選択もPC版のまま保持）。- **左列の幅固定（PC版）**：PC版（自動縮小時を含む）では左列の幅を固定し、セッション名未入力時のプレースホルダー「クリックしてタイトルを入力」が必ず1行に収まる幅を確保する（キャラクター編集などの操作でも左列・各パネルの幅は変化しない）。- **操作アイコンの右端揃え**：幅が最短のカードの操作アイコン右端を基準に、全カードのアイコン群の右端を揃える（PC版・スマホ版それぞれで再計算）。

---

## 5. キャラクター管理

- **追加**：キャラ名と色を選んで登録。名前は全角スペース等を整えたうえで、**既存キャラと重複する名前は登録できない**。
  - **入力フォーム**：「キャラ名」入力欄→色選択→「追加」を中央揃えで並べる。キャラ名の入力文字は左揃えで表示する。
- **カラーパレット**：色ボタンからパレットを開いて色を選択（14色のプリセット）。
- **キャラクター名編集**：✒️ でインライン編集（✓保存／↩︎取消）。改名時も重複チェックを行う。
  - **改名の追従**：改名すると、全カードの登場人物・絞り込みの選択・比較ビューの並び順・入力フォームの人物選択が旧名から新名へ自動で置き換わる。
  - **編集中のレイアウト保持**：キャラクター名の編集を開始しても、左列（キャラクター一覧・セッション名など）のパネル幅は変化せず、編集前と同じ大きさを保つ。
- **色変更**：🎨 で色を変更（変更は即保存され、カード上のバッジ色にも反映）。
- **色丸（カラードット）**：キャラクター一覧の色丸の枠線は、白ではなく**それぞれの色の明度を下げた同系色**で表示する。
- **削除**：✕ で個別削除（確認ダイアログあり）。削除したキャラクターは各カードの登場人物から外れるが、**カード自体は残る**。
- **🔓 / 🔐 ロック**：🔓（解除中）を押すとキャラクターをロックし、名前・色の編集と削除を防ぐ（誤操作防止）。ロック中は 🔐 を押すと解除。
- カードのバッジ色・カード色はキャラクター色から動的生成される CSS で反映。

---

## 6. 秘密のメモ

- 自由記述のテキストエリア。入力内容はデバウンスして自動保存。
- **👁️ 表示/非表示トグル**：メモ全体を隠す。
  - 非表示中は「（非表示中）」ラベルを表示。
  - 非表示中の 👁️ ボタンは、カードの非表示ボタンと同じ控えめな暗い青灰色・半透明で表示（挙動を統一）。

---

## 7. 時系列カードの登録

- 入力項目：**時間 / 人物（複数可） / できごと**。
- **できごとは必須**。空欄では登録されない。
- **時間の自動補完・正規化**：
  - 全角数字は半角に自動変換。
  - 入力中に 4 桁そろうと `1030` → `10:30` のように「:」を自動挿入。
  - フォーカスを外すと `915` → `9:15`、`10:5` → `10:05` のように正規化。
- **自由記述の時刻**：「夕方」「翌朝」「不明」など時刻形式でない文字も登録可能。
  - 時刻形式は早い順、自由記述は末尾にまとまる。
- **並び順**：時間の早い順に自動整列。
- **登場人物**：
  - 「人物 ▾」から複数選択可能。選択数に応じてボタンの文言が変わる（例：「2人選択 ▾」）。
  - キャラクターが未登録の場合は「先に登録してください」と案内を表示。
  - 空欄でも登録可（名前・色なし。列位置は揃う）。
- **🚩 重要フラグ**：入力欄のフラグを押してから追加すると、フラグ付きで登録。
- **追加後の入力欄リセット**：追加を実行すると、時間・終了時刻・できごと・フラグに加えて、**人物の選択と「⛱ 期間」モードも初期状態（未選択・期間オフ）に戻る**。

---

## 8. 期間（開始〜終了）カード

- 入力欄の **「⏱ 期間」** で終了時刻欄を表示し、開始〜終了の期間として登録。
- **単一カード表示**：期間の途中に他の出来事がない場合、「開始〜終了」を1枚のカードで表示（右端に人物色の縦バー）。
- **分割表示（コネクタ）**：期間の途中に別の出来事があると、開始カードと終了カードに分割し、右側の縦ライン（コネクタ）でつなぐ。
  - コネクタが入れ子になる場合はレーンを分けて階層表示。
  - コネクタの線幅・階層間隔・カードとの間隔を調整済み。
- **コネクタ終点カードの時間色**：薄いグレーで表示。
- 人物・内容・フラグは開始カードに保持。終了時刻も時刻として扱われる。
- 編集で終了時刻を後から変更・追加・削除可能（終了欄を空にすると単一時刻カードに戻る）。

---

## 9. カード操作（リストビュー）

各カードに以下の操作を用意：

- **時間バッジのクリック編集**：時間バッジをクリックすると、そのカードの**時間だけ**をその場でインライン編集できる（Enter で保存、Esc でキャンセル、フォーカスを外しても保存）。内容・終了時刻・期間はそのまま。
- **✏️ 編集**：時間・終了時間・内容をまとめて編集（✓保存／↩︎取消）。できごと欄で Enter を短時間に 2 回押しても保存できる。
  - ✓（保存）は緑背景・↩︎（取消）は淡いグレー背景で、両ボタンとも同じ高さ（34px）に揃える。
- **👥 登場人物**：カードに人物を後から追加・削除（ポップアップは開いたまま連続で操作できる）。
- **🚩 フラグ**：カードごとの重要フラグ切り替え（左端に蛍光イエローの帯）。
- **👁️ 非表示**：内容を隠す（👁️と🚩以外を非表示）。
  - 非表示中は 🎨 でカード色をグレーに隠すことも可能。
- **✕ 削除**：カードを削除。
- **⬆︎ / ⬇︎ 移動**：時間なし（不詳）カードのみ表示。タイムライン上を上下に移動可能。
  - スマホ版でカード幅が狭くボタンが折り返す場合は、⬆︎⬇︎ を ✕ の右隣に自動で再配置する。
- **削除確認トグル**：「カード削除時に確認画面を表示」で、✕削除時の確認ダイアログの有無を切り替え（既定はON）。

### 人物バッジの表示

- **1人**：その人物の色のバッジを表示。
- **複数人**：バッジを縦に並べて表示。**PC版では 3 人以上のとき 2 人まで表示**し、「＋他を表示」ボタンで残りを展開（展開中は「－ 閉じる」）。
- **スマホ版**：折りたたまず全員のバッジを横並び（折り返しあり）で表示。
- バッジの文字色は背景色の知覚輝度に応じて黒／白を自動選択。

### 色・スタイル

- **1人のカード**：その人物の色でカード全体を色分け。
- **複数人物のカード**：薄い青の背景で表示。
- **フラグ**：左端に幅4pxの蛍光イエローの帯（色 `#eaff00`）。キャラクターの黄・オレンジ系の色と見分けられるよう、帯とカードの間に幅1pxの白い区切り線を挟む。
- **死亡推定時刻の範囲内**：カードの左外側に2px離して、幅5px・角丸の赤バー（色 `#C00000`）を表示。カードから物理的に離すので、赤いキャラのカードやフラグ帯とも色が混ざらない。連続する範囲内カードの赤バーは一本につながって見える。

---

## 10. 死亡推定時刻

- 時系列右上のプルダウンで開始〜終了の時刻（分は 00 / 15 / 30 / 45 から選択）を選び、**ON** にすると、その範囲内の出来事の左端（カード外側）に赤バー（`#C00000`）を表示。ONボタンも赤系（`#b91c1c`）で状態を示す。
- 時・分の各プルダウンには未選択（`--`）があり、開始・終了のどちらかが未確定の間は範囲が無効になる。
- 開始＞終了の場合は日付跨ぎ（例：23:00〜翌1:00）として扱い、警告を表示。
- 人物比較ビューでは、範囲内の時刻ラベルを赤字で表示。

---

## 11. 絞り込み

- **表示対象絞り込み**：
  - 人物を選ぶと、その人物が登場するカードのみ表示（複数選択可。いずれかの人物が登場すれば表示）。
  - 「全員」で解除。
  - 「🚩 フラグ付き」でフラグ付きカードのみ表示（フラグなしのみの絞り込みにも対応）。
  - 人物絞り込みとフラグ絞り込みは **AND 条件**で併用できる。

---

## 12. 表示モード（リスト / 人物比較）

### 📋 リスト表示

- 時系列を縦一列のカードで表示（標準表示）。

### 📊 人物比較表示

- **左端に時間、上に人物名**を並べ、各人物のその時刻のカードを対応する欄に表示。
- 「誰がいつ何をしていたか」を横並びで比較可能。
- 並ぶ人物は表示対象絞り込みで選んだ人（「全員」時は全員分の列）。
- 同一人物欄内で期間が重なる場合はレーン（サブ列）に分けて表示。期間カードと通常カードは別レーン。
- 期間イベントは開始〜終了の行をまたぐ塗りセル（rowspan）で表示。
- **各カードの右下**に操作ボタンを配置（✏️ 編集 / 🚩 フラグ / 👁️ 表示・非表示 / ✕ 削除）。
- ✏️ 編集では時間・終了時刻・内容を編集でき、Enter を短時間に 2 回押すと保存、Esc で取消。
- 列見出し（人物名）は人物色で塗り分け、文字色は背景の知覚輝度に応じて黒／白を自動選択。
- **カードの大きさと操作領域**：カードの高さ・幅は固定値で切り詰めず、カード内に実際に描画される内容、操作ボタンの数と各ボタンの大きさ、ボタン間の間隔、内容と操作ボタンの間隔、カード内の上下左右のパディングを含む自然な大きさにする。表示する操作ボタンが増減した場合も、すべてのボタンがカード内に収まり、カード外にはみ出さず、内容や他のボタンと重ならないようにする。
- **操作の成立性**：人物比較ビューに表示される各操作ボタン（✏️ 編集 / 🚩 フラグ / 👁️ 表示・非表示 / ✕ 削除）は、カード内のクリック可能な領域として描画する。各ボタンを押したとき、対象カードを取り違えずに対応する処理が実行され、編集・フラグ切り替え・表示切り替え・削除の結果が保存され、必要な範囲が再描画される。ボタンがカード外へはみ出すこと、別カードやセルに覆われて押せないこと、カード外のクリックとして誤処理されることがない。
- **状態変化後の維持**：ボタン操作、カード編集、絞り込み、非表示設定、表示倍率変更、期間カードの再構成後も、カードの大きさを再計算し、描画されているすべての操作ボタンがカード内に収まる状態を維持する。
- **列（人物）の並び替え**：各人物列の見出しにある **⬅ / ➡** ボタンで、比較ビュー上の人物の並び順を左右に入れ替え可能。
  - 並び順は**比較ビュー専用**で、キャラクター一覧の順序には影響しない。
  - 未登録の人物は自動でキャラクター一覧の順に末尾へ補完（正規化）される。
- **「人物の並びを固定する」トグル**：
  - ONにすると、比較ビューで並べ替えた人物の順番を保存し、リスト表示に戻っても維持する。
  - OFFのときは、比較ビューに入るたびに並び順をリセット（キャラクター一覧の順）する。
  - 状態・並び順は保存される。
- **「非表示設定中アイテムを不可視化」トグル**（人物比較ビュー時のみ表示）：
  - ONの間、非表示フラグが付いたカードを比較ビューから完全に除外。
  - OFFにすると再び表示。
  - 状態は保存される。
- **表示倍率（ズーム）**（人物比較ビュー時のみ表示）：
  - **− / ＋** ボタンで 10%〜200% の範囲で 10% 刻みに拡大・縮小。「リセット」で 100% に戻す。
  - 比較テーブルのみに適用され、他の画面には影響しない。倍率は保存される。

---

## 13. ヘルプ

- 「？」ボタンで使い方モーダルを表示。✕ ボタンまたはモーダル外側のクリックで閉じる。
- 基本の登録方法、各ボタンの機能、複数人物と絞り込み、リスト/人物比較、期間登録、空欄登録の挙動などを解説。

---

## 14. 保存されるデータ（localStorage キー）

| 項目 | キー |
| --- | --- |
| 時系列イベント | `madamisu_events` |
| キャラクター | `madamisu_characters` |
| 絞り込み人物 | `madamisu_char_filter` |
| 秘密のメモ | `madamisu_memo` |
| セッション名 | `madamisu_session_name` |
| メモ非表示状態 | `madamisu_memo_concealed` |
| 死亡推定時刻（開始） | `madamisu_death_from` |
| 死亡推定時刻（終了） | `madamisu_death_to` |
| 死亡推定時刻 ON/OFF | `madamisu_death_enabled` |
| 表示モード（PC/スマホ） | `madamisu_view` |
| 時系列表示モード（list/compare） | `madamisu_timeline_mode` |
| フラグ絞り込み | `madamisu_flag_filter` |
| 削除確認の有無 | `madamisu_confirm_delete` |
| 比較ビューで非表示除外 | `madamisu_show_concealed_compare` |
| 比較ビュー専用の人物並び順 | `madamisu_compare_order` |
| 比較ビューの並び順を固定 | `madamisu_compare_order_fixed` |
| 比較ビューの表示倍率 | `madamisu_compare_zoom` |
| セッションデータ欄の開閉状態 | `madamisu_session_data_expanded` |
