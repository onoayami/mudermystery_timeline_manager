# コードレビュー結果（再レビュー版）

**対象ファイル:** `マダミス時系列整理.html`（1098行）
**構成:** 単一HTMLファイル（クライアントサイドのみ / localStorage による永続化）
**レビュー日:** 2026年7月22日

> **前提メモ:** データベースは使用していないため、観点9（N+1問題などのDBクエリ効率）は非該当です。

---

## 重大度サマリ

| 重大度 | 件数 | 概要 |
| --- | --- | --- |
| 🔴 致命的 | 1件 | 時刻フィールド経由の格納型XSS（エスケープ漏れ） |
| 🟡 警告 | 1件 | インラインstyleへの color 値が未検証（一貫性・防御的観点） |
| 🔵 提案 | 3件 | デッドコード削除、未使用変数、部分更新時のインデックス整合性 |

---

## 前回指摘の対応状況（すべて反映済み ✅）

| 前回# | 内容 | 状況 |
| --- | --- | --- |
| 1 | 本文XSS | ✅ `escapeHtml(ev.text)` に修正（1090行目） |
| 2 | キャラ名XSS | ✅ `escapeHtml(char.name)`（560行目） |
| 3 | CSSインジェクション | ✅ `char-idx-${i}` + 色を `#RRGGBB` 検証（757〜765行目） |
| 4 | 読込例外 | ✅ `loadJSON` で try/catch |
| 5 | 書込例外 | ✅ `safeSet`（保存失敗の警告は1回のみ） |
| 6 | 名前 trim / 重複 | ✅ `.trim()` 追加 |
| 7 | フル再描画 | ✅ `refreshCard` / `persistEvents` で軽量更新 |
| 8 | 線形探索 | ✅ `colorMap` / `indexMap` で O(1) 参照 |
| 9 | 輝度判定 | ✅ 知覚輝度 + NaN対策 |
| 10 | マジック値 | ✅ `KEYS` に集約 |
| 11 | 時刻逆転 | ✅ 日跨ぎ対応 + 警告表示 |
| 12 | デバウンス | ✅ メモ保存を300msデバウンス |

> 前回指摘への対応品質は高い。特に #11 は日跨ぎ範囲（`f<=to ? … : …`）まで実装されており丁寧。

---

## 🔴 致命的

### A. 時刻フィールド経由の格納型XSS（エスケープ漏れ）

- **該当箇所:** `マダミス時系列整理.html` 1072行目
- **重大度:** 🔴 致命的

```js
<div class="time-badge">${ev.time || '不詳'}</div>
```

**指摘内容:**
本文（`ev.text`）はエスケープ対応されたが、**時刻バッジ `ev.time` が未エスケープのまま**残っている。
「時刻だから安全」と見逃されがちだが、`normalizeTime()`（1013〜1032行目）は `:` を含まず桁数が3/4でない入力を **そのまま返す** ため、危険な文字列がすり抜ける。

**再現手順:**
1. 時間欄に `<img src=x onerror=alert(1)>` を入力して追加する
2. `normalizeTime` はコロンなし・数字1桁のため、入力文字列をそのまま返す
3. `ev.time` に HTML が保存され、`time-badge` に生挿入 → **スクリプト実行**

編集フォームの value 側は `escapeHtml(ev.time)` 済みのため、表示バッジだけが穴になっている。

**修正案:**

```js
<div class="time-badge">${escapeHtml(ev.time || '不詳')}</div>
```

併せて `normalizeTime` の最終 `return s;` を、不正入力なら空文字を返す形にするとより堅牢。

---

## 🟡 警告

### B. インラインstyleへの `color` 値が未検証（一貫性・防御的観点）

- **該当箇所:** 559行目（char-color-dot）、614行目 / 723行目（dot）、673行目（filter-chip）、1003行目（data-color）、`renderEvents` のバッジ生成（1057行目付近）
- **重大度:** 🟡 警告

**指摘内容:**
`updateCardColorCss()` では色を `/^#[0-9a-fA-F]{6}$/` で検証するようになったが、**インラインstyleに差し込む多数の箇所は未検証**のまま。
色はパレット由来なので通常は安全だが、localStorage を改変された場合や将来の入力経路追加時に、`"` を含む値で属性を抜けられる可能性がある（`escapeHtml` はダブルクォートを変換しない）。検証ロジックが一部だけにあるのは一貫性の面でも弱い。

**修正案:** 検証を関数化して全箇所で共通利用する。

```js
function safeColor(c) {
  return /^#[0-9a-fA-F]{6}$/.test(c) ? c : '#94a3b8';
}
// 例: style="background:${safeColor(char.color)}"
```

---

## 🔵 提案

### C. デッドコード：`buildCharOptions` が未使用

- **該当箇所:** 999〜1006行目（呼び出し元なし）、関連CSS `.edit-char`
- **重大度:** 🔵 提案

**指摘内容:**
編集モードが時間・内容のみになった結果、`buildCharOptions()` はどこからも呼ばれていない。混乱の元なので削除を推奨。

---

### D. `toggleBadgeExpand` の未使用変数

- **該当箇所:** 696行目
- **重大度:** 🔵 提案

```js
const total = wrap.querySelectorAll('.char-badge').length; // 未使用
```

**修正案:** 行ごと削除する。

---

### E. 部分更新時のインデックス整合性（注意点）

- **該当箇所:** `deleteEvent`（960行目付近）、`refreshCard`（`data-index` 依存）
- **重大度:** 🔵 提案

**指摘内容:**
フラグ・非表示・色は `refreshCard` で軽量更新されるようになったが、削除は `saveEvents()`（全再描画）のまま。削除はインデックスがずれるため全描画が妥当で、これ自体は問題なし。
**注意点** として、`refreshCard` は `data-index` に依存するため、今後「並べ替えを伴わない部分更新」を増やす際はインデックス整合性に注意すること。

---

## 各観点まとめ

| 観点 | 評価 |
| --- | --- |
| 1. バグ / 論理エラー | 良好（日跨ぎ対応済み）。残る実害は **A の時刻XSS** のみ |
| 2. セキュリティ | 本文・名前・CSSは対策済み。**時刻の1点が残存（🔴A）**、色検証の一貫性（🟡B） |
| 3. パフォーマンス | `refreshCard` / `colorMap` で改善済み。良好 |
| 4. 可読性 / 命名規則 | 良好。`KEYS` 集約・コメント丁寧。デッドコードのみ整理余地 |
| 5. エラーハンドリング | `loadJSON` / `safeSet` で堅牢化。良好 |
| 6. 時間計算量（Big-O） | O(n log n) ソート＋O(n) 描画。適切 |
| 7. 不要なループ・再計算 | 大幅改善。未使用変数 / 関数のみ残 |
| 8. メモリ使用量 | 問題なし（innerHTML 入替でリスナーも GC 対象） |
| 9. DBクエリ効率 | 非該当（localStorage のみ） |
| 10. キャッシュ活用 | メモのデバウンス導入済み。良好 |

---

## 総評

- 前回指摘への対応品質は高く、設計も明快。
- **残る最優先は 🔴A（時刻フィールドのXSS）の1点のみ。** 本文を塞いだ一方で時刻が漏れているため、`escapeHtml(ev.time || '不詳')` の1行で確実に閉じられる。
- 続いて **🟡B の色検証共通化** で防御を揃えれば、セキュリティ面はほぼ盤石。
- 🔵C〜E は動作への影響はなく、コード整理・保守性向上のための任意対応。
