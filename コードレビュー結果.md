# コードレビュー結果（v3）

**対象ファイル:** `マダミス時系列整理.html`（1785行）
**構成:** 単一HTMLファイル（クライアントサイドのみ / localStorage による永続化）
**レビュー日:** 2026年7月23日

> **前提メモ:** データベースは使用していないため、観点9（N+1問題などのDBクエリ効率）は非該当です。

---

## 重大度サマリ

| 重大度 | 件数 | 概要 |
| --- | --- | --- |
| 🔴 致命的 | 1件 | `escapeHtml` がクォート未変換 → 属性インジェクションXSS |
| 🟡 警告 | 1件 | `alignControlsToShortestCard` のレイアウトスラッシング（強制reflow） |
| 🔵 提案 | 3件 | デッドコード、比較モードの O(n²) 探索、期間判定の O(n²) |

---

## 前回（v2）指摘の対応状況 ✅

| 前回# | 内容 | 状況 |
| --- | --- | --- |
| A | 時刻バッジのXSS | ✅ `escapeHtml(ev.time || '不詳')` に修正（1591行目 ほか） |
| B | インラインstyle の色未検証 | ✅ `safeColor()`（820行目）を全styleとCSSに適用 |
| C | デッドコード `buildCharOptions` | ✅ 削除済み |
| D | 未使用変数 `total` | ✅ 削除済み |

> 大幅な機能追加（期間イベント／人物比較表示／キャラ名編集／データ削除／不詳カード並べ替え）も、概ね堅実な実装。

---

## 🔴 致命的

### 1. `escapeHtml` がクォートを変換せず、属性インジェクションでXSS

- **該当箇所:** 定義 1338〜1340行目 / 注入箇所 801行目（キャラ名編集の value）、1555行目（title）、1574〜1577行目（編集フォームの value）
- **重大度:** 🔴 致命的

```js
function escapeHtml(s) {
  return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}
```

**指摘内容:**
`escapeHtml` は `& < >` のみ変換し、**`"`（ダブルクォート）を変換していない**。その出力を **HTML属性値の中**（`value="..."`, `title="..."`）に差し込んでいるため、値に `"` を含めると属性を抜けて新しい属性を注入できる。`<` `>` は封じられているが、**イベントハンドラ属性の注入だけで任意JS実行が可能**。

**再現手順:**
1. できごと欄に次を入力して追加:
   `" autofocus onfocus="alert(document.cookie)`
2. そのカードの ✏️ 編集を押す
3. 編集フォームがこう描画される:
   ```html
   <input class="edit-text" value="" autofocus onfocus="alert(document.cookie)" placeholder="内容">
   ```
4. `autofocus` + `onfocus` により **スクリプトが即実行**（秘密のメモ等を窃取可能）

キャラクター名に `"` を含めても同様に発火する（801行目の value、1555行目の title）。

**修正案:** `escapeHtml` を属性安全にする（1箇所直せば全注入箇所が塞がる）。

```js
function escapeHtml(s) {
  return String(s)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;')
    .replace(/'/g, '&#39;');
}
```

---

## 🟡 警告

### 2. `alignControlsToShortestCard` のレイアウトスラッシング

- **該当箇所:** 1631〜1648行目
- **重大度:** 🟡 警告

```js
controls.forEach(c => {
  const offset = Math.round(anchorRight - c.getBoundingClientRect().right); // 読み
  const inner = innerByControls.get(c);
  if (pcView && inner && offset < 0) inner.style.paddingRight = `${-offset + 8}px`; // 書き
  const adjustedOffset = Math.round(anchorRight - c.getBoundingClientRect().right); // 直後に再読み → 強制リフロー
  c.style.transform = adjustedOffset ? `translateX(${adjustedOffset}px)` : '';
});
```

**指摘内容:**
ループ内で **DOMスタイル書き込み → 直後に `getBoundingClientRect()` 読み取り** を繰り返しており、反復ごとに **強制同期レイアウト（reflow）** が発生する（O(カード数)回のリフロー）。この関数は `renderEvents()` と `setView()` のたびに走るため、カードが増えると描画が目に見えて重くなる。

**修正案:** 「読み取り一括 → 書き込み一括」に分離する。

```js
// 1) まず全カードの right をまとめて読む
const rects = controls.map(c => c.getBoundingClientRect().right);
const anchorRight = Math.min(...rects);
// 2) paddingRight をまとめて書く
controls.forEach((c, i) => {
  const inner = innerByControls.get(c);
  const offset = Math.round(anchorRight - rects[i]);
  if (pcView && inner && offset < 0) inner.style.paddingRight = `${-offset + 8}px`;
});
// 3) 再度まとめて読み、まとめて transform を書く
const rects2 = controls.map(c => c.getBoundingClientRect().right);
controls.forEach((c, i) => {
  const adj = Math.round(anchorRight - rects2[i]);
  c.style.transform = adj ? `translateX(${adj}px)` : '';
});
```

---

## 🔵 提案

### 3. デッドコード：`darkenColor` が未使用

- **該当箇所:** 1478〜1485行目
- **重大度:** 🔵 提案

**指摘内容:** `darkenColor()` はどこからも呼ばれていない（`rangeColor` は使用中）。混乱防止のため削除を推奨。

---

### 4. 人物比較の `events.indexOf(ev)` による O(n²) 探索

- **該当箇所:** `compareActions` 内 1683行目
- **重大度:** 🔵 提案

```js
const compareActions = ev => {
  const index = events.indexOf(ev); // セルごとに O(n) 線形探索
  ...
};
```

**指摘内容:**
比較表の各セル描画ごとに `events.indexOf` を呼ぶため、全体で概ね **O(セル数 × n)**。同モードでは `colorForChar(name)`（810行目の `find`）もセルごとに O(m) で走る。件数が増えると効いてくる。

**修正案:** 事前に `Map` を作って O(1) 参照にする。

```js
const indexOfEv = new Map(events.map((e, i) => [e, i]));
const colorMap = new Map(characters.map(c => [c.name, c.color]));
// compareActions 内: const index = indexOfEv.get(ev);
// 色引き: safeColor(colorMap.get(name))
```

---

### 5. `buildSortedRows` の期間判定が O(n²)

- **該当箇所:** 1382〜1417行目付近（`markers.some(...)` を各行で実行）
- **重大度:** 🔵 提案

**指摘内容:**
期間イベントごとに全 `markers` を走査（`markers.some`）するため最悪 O(n²)。通常件数なら実害は小さいが、`markers` を時刻ソートして二分探索、または「区間に点が1つでもあるか」を事前集計すると O(n log n) にできる。任意対応。

---

## 各観点まとめ

| 観点 | 評価 |
| --- | --- |
| 1. バグ / 論理エラー | 良好。期間分割・不詳カード移動・日跨ぎ判定は妥当 |
| 2. セキュリティ | 本文・時刻・色は対策済みだが、**属性クォート未エスケープでXSS残存（🔴1）** |
| 3. パフォーマンス | 概ね良好。**reflowスラッシング（🟡2）** と比較モードの O(n²)（🔵4/5） |
| 4. 可読性 / 命名規則 | 良好。コメント充実。デッドコード（🔵3）のみ |
| 5. エラーハンドリング | `loadJSON` / `safeSet`、削除系も try/catch。良好 |
| 6. 時間計算量（Big-O） | リストは O(n)〜O(n log n)。比較モードに O(n²) 要素あり |
| 7. 不要なループ・再計算 | reflow多発（🟡2）、indexOf / find の反復（🔵4） |
| 8. メモリ使用量 | 問題なし（innerHTML 入替でリスナーも GC 対象） |
| 9. DBクエリ効率 | 非該当（localStorage のみ） |
| 10. キャッシュ活用 | メモはデバウンス済み。比較モードは Map 化の余地（🔵4） |

---

## 総評

- 機能追加の実装は堅実で、前回（v2）指摘もすべて反映済み。
- **最優先は 🔴1（属性インジェクションXSS）。** `escapeHtml` に `"` / `'` の変換を1箇所加えるだけで、value・title の全注入箇所が同時に塞がる。編集フォーム経由で確実に発火するため、必ず対応を推奨。
- 次点で 🟡2 の reflow 改善。カード数が多い実利用でのもたつき解消に効く。
- 🔵3〜5 は動作への影響はなく、コード整理・比較モードの高速化のための任意対応。
