# コードレビュー結果｜修正コミット 467088e 再レビュー

## 総合判定

**要再修正（現時点では完了扱い不可）**

- 対象：`マダミス時系列整理.html`
- コミット：`467088e910abd82d6ad75e65f3f4210e0bfddeca`
- blob SHA：`25c565bc85b2a7f7703446cb7380e6c9b72b26ee`
- 確認：差分、保存経路の全件検索、Chromiumでの2タブ競合・寸法・フォーカス実測

最優先条件だった「古いタブからの一括操作が別タブの新しい更新を上書きしない」が未達です。タブBで追加した出来事が、古いタブAの「全キャラクター削除」で実際に消失しました。

## 指摘事項

### 1. 一括削除・インポートが競合保護を迂回

- **重大度：🔴 致命的**
- **該当箇所：1673〜1778行付近**

```javascript
function clearAllCharacters() {
  safeSet(KEYS.events, JSON.stringify(events));
  localStorage.removeItem(KEYS.characters);
}
function clearAllTimelineData() {
  localStorage.removeItem(KEYS.events);
}
```

- **指摘内容：1. 最優先だったデータ消失経路が残っています。**

`importData()`、`clearAllData()`、`clearAllCharacters()`、`clearAllTimelineData()`が`mutateStorage()`を通りません。実測では操作前`rev=3 / events=["Bで追加した新しい出来事"]`から、古いタブの全キャラクター削除後に`rev=3 / events=[]`となりました。

- **機能条件：** 古いタブの一括削除・インポートが新しいタブの更新を上書きせず、複数キー更新が一まとまりで成功すること。
- **修正案：**
  1. **最優先：4処理をすべて`mutateStorage()`へ統合する。** 確認漏れを構造的に防げます。

```javascript
function removeKeyOrThrow(key) {
  try { localStorage.removeItem(key); }
  catch (error) { throw new Error(`${key} の削除に失敗`, { cause: error }); }
}

function clearAllCharacters() {
  if (!confirm('登録したキャラクターをすべて削除します。よろしいですか？')) return;
  const nextEvents = events.map(event => ({ ...event, characters: [], character: '' }));
  const committed = mutateStorage(() => {
    if (!safeSet(KEYS.events, JSON.stringify(nextEvents))) throw new Error('events');
    [KEYS.characters, KEYS.charFilter, KEYS.compareOrder,
     KEYS.playerCharacter, KEYS.charRegDone].forEach(removeKeyOrThrow);
  });
  if (!committed) return;
  events = nextEvents;
  skipMemoFlush = true;
  location.reload();
}
```

  2. **次に：インポートも世代確認後に一括置換する。** 古いタブからの取り込みで新規データを消さないためです。

```javascript
function commitImportedPayload(payload) {
  const keys = Object.values(KEYS).filter(key => key !== KEYS.rev);
  return mutateStorage(() => {
    keys.forEach(removeKeyOrThrow);
    for (const key of keys) {
      if (!(key in payload)) continue;
      if (typeof payload[key] !== 'string' || !safeSet(key, payload[key])) {
        throw new Error(`${key} の取り込みに失敗`);
      }
    }
  });
}
```

  3. **2タブ回帰テストを4処理すべてへ追加する。**

### 2. 競合後の編集ロックが機能していない

- **重大度：🔴 致命的**
- **該当箇所：1019〜1031行、1078〜1100行付近**

```javascript
let editingLockedByConflict = false;
function lockEditingAfterConflict() {
  editingLockedByConflict = true;
}
```

- **指摘内容：2. ロック変数は代入されるだけで、変更操作側が確認していません。**

競合警告後も一括削除、インポート、セッション名、死亡推定時刻、人物選択などを操作できます。`storage`イベントでも警告表示だけでロックされません。

- **機能条件：** 競合後は再読み込み・書き出し以外の変更操作を停止すること。
- **修正案：**
  1. **警告とロックを統合する。**

```javascript
let editingLocked = false;
function enterConflictMode() {
  if (editingLocked) return;
  editingLocked = true;
  showStorageConflictBar();
  document.querySelectorAll('button, input, select, textarea').forEach(element => {
    if (!element.matches('[data-allow-when-locked]')) element.disabled = true;
  });
}
window.addEventListener('storage', event => {
  if (event.key === null || Object.values(KEYS).includes(event.key)) enterConflictMode();
});
```

  2. **全変更関数の先頭で`canEdit()`を確認する。** DOM以外の呼び出しも止められます。
  3. **書き出し・再読み込みだけをロック対象外にする。**

### 3. 復元失敗を検知できず成功と誤案内

- **重大度：🔴 致命的**
- **該当箇所：1011〜1043行付近**

```javascript
try { localStorage.setItem(key, value); } catch (e) {}
```

- **指摘内容：3. 復元エラーを握り潰すため、途中までしか戻っていなくても「保存前へ戻しました」と表示します。**

- **機能条件：** 復元失敗時は不整合の可能性を明示し、編集を止めること。
- **修正案：**
  1. **復元結果を返す。**

```javascript
function restoreSnapshot(snapshot) {
  const errors = [];
  for (const key of Object.values(KEYS)) {
    try { localStorage.removeItem(key); }
    catch (error) { errors.push({ key, phase: 'remove', error }); }
  }
  for (const [key, value] of Object.entries(snapshot)) {
    if (value === null) continue;
    try { localStorage.setItem(key, value); }
    catch (error) { errors.push({ key, phase: 'restore', error }); }
  }
  return { ok: errors.length === 0, errors };
}
```

  2. **失敗時は専用警告・編集停止・書き出し案内を出す。**
  3. **`setItem`／`removeItem`の途中失敗テストを追加する。**

### 4. 保存結果を無視して未保存状態を表示

- **重大度：🟡 警告**
- **該当箇所：2382〜2393行、2697〜2706行、3880〜3891行、メモ保存**

```javascript
guardedSet(KEYS.charFilter, JSON.stringify(charFilter));
renderEvents();
```

- **指摘内容：4. `guardedSet()`が失敗しても画面を更新します。** 絞り込み、比較順、メモなどが保存済みに見え、再読み込み後に消えます。
- **機能条件：** 成功後だけ状態を確定し、失敗時は直前状態へ戻すか未保存表示にすること。
- **修正案：**
  1. **変更前と変更後を分け、成功後だけ代入する。**

```javascript
const previous = [...charFilter];
const next = buildNextFilter(previous, name);
if (!guardedSet(KEYS.charFilter, JSON.stringify(next))) {
  charFilter = previous;
  renderCharFilter();
  return;
}
charFilter = next;
renderCharFilter();
```

  2. **メモへ「保存中／保存済み／未保存」を表示する。**
  3. **`guardedSet()`の戻り値無視を全件検索して0件にする。**

### 5. 保存不可環境でも編集できる

- **重大度：🟡 警告**
- **該当箇所：1045〜1051行、`init()`**

- **指摘内容：5. `canUseLocalStorage()`失敗時は警告だけで、編集を止めません。** 再読み込み時に入力が消えます。
- **機能条件：** 保存不可なら常時警告し、書き出し以外の変更操作を停止すること。
- **修正案：**
  1. **`enterStorageFailureMode()`で入力・変更ボタンを無効化する。**
  2. **書き出しだけは利用可能にする。** 現在内容を救出するためです。

### 6. タッチ対象とフォーカス修正が不完全

- **重大度：🟡 警告**
- **該当箇所：187〜209行、638行、683〜686行付近**

実測：死亡時刻ラベル編集19×15.2px、死亡時刻select 24×44px、メモ切替39×25.2px、ヘルプ閉じる30×30px。

```css
.death-range select:focus { outline: none; }
```

- **指摘内容：6. 主要操作が44px未満で、死亡時刻selectのフォーカス枠も消えたままです。**
- **機能条件：** 主要タッチ対象を原則44×44px以上にし、キーボード位置を常に見えるようにすること。
- **修正案：**
  1. **`outline: none`を削除する。**
  2. **未対応操作へ最小寸法を追加する。**

```css
.death-label-edit-btn, .memo-conceal-btn, .help-close {
  min-width: 44px; min-height: 44px;
  display: inline-flex; align-items: center; justify-content: center;
  box-sizing: border-box;
}
.death-range select { min-width: 44px; min-height: 44px; }
.death-range select:focus-visible {
  outline: 3px solid #facc15; outline-offset: 3px;
}
```

  3. **操作間に原則8pxの間隔を確保する。**
  4. **390／768／1440px・200%相当で再実測する。**

### 7. 入力名・必須状態・アイコン状態が不足

- **重大度：🟡 警告**
- **該当箇所：入力部、アイコンボタン、切替ボタン、人物選択行**

- **指摘内容：7. `eventInput`の必須属性、`endTimeInput`のラベル、アイコンの`aria-label`、切替の`aria-pressed`が不足しています。** クリック可能な人物行も`div`のままです。
- **機能条件：** 入力目的と操作名・状態がキーボード・支援技術へ伝わること。
- **修正案：**
  1. **ラベルと必須属性を追加する。**

```html
<label for="endTimeInput" class="sr-only">終了時刻</label>
<input id="endTimeInput" type="text">
<label for="eventInput" class="sr-only">できごと（必須）</label>
<input id="eventInput" type="text" required aria-required="true">
```

  2. **アイコンに`aria-label`、切替に`aria-pressed`を付け、状態変更時に更新する。**
  3. **クリック可能な`div`を`button type="button"`へ置換する。**

### 8. 存在しない人物参照がイベントに残り得る

- **重大度：🟡 警告**
- **該当箇所：1119行以降の正規化、1322行以降の初期状態**

- **指摘内容：8. `charFilter`と`compareOrder`は絞り込まれますが、イベント内人物名と`playerCharacter`の孤立参照を起動時に一括除去する処理が明確ではありません。**
- **機能条件：** 壊れた取り込みデータでも不整合を残さず安全に起動すること。
- **修正案：**
  1. **人物名集合で全参照を正規化する。**

```javascript
function normalizeCharacterReferences(state) {
  const known = new Set(state.characters.map(character => character.name));
  const keep = names => normalizeStringArray(names).filter(name => known.has(name));
  return {
    ...state,
    events: state.events.map(event => {
      const names = keep(event.characters);
      return { ...event, characters: names, character: names[0] || '' };
    }),
    charFilter: keep(state.charFilter),
    compareOrder: keep(state.compareOrder),
    playerCharacter: known.has(state.playerCharacter) ? state.playerCharacter : null
  };
}
```

  2. **補正結果を保存する場合も競合保護する。**
  3. **配列以外・異種要素・重複・孤立参照の組合せをテストする。**

### 9. 必須テスト・実ブラウザ検証が未実施

- **重大度：🟡 警告**
- **該当箇所：テスト工程、完了報告§10**

- **指摘内容：9. 必須テストが未実施のままpushされています。** テストがあれば今回の競合保護漏れとフォーカス漏れを検出できました。
- **機能条件：** 2タブ、途中失敗、破損データ、人物参照、時刻、アクセシビリティ、画面幅のテストが成功すること。
- **修正案：**
  1. **リポジトリ直下へ`tests/`とPlaywright設定を追加する。** 配置場所は実装上決められます。
  2. **一括削除・インポート、復元失敗、壊れたJSON、人物改名、時刻境界、Esc・Tab、44px、390／768／1440pxを自動化する。**
  3. **総数、PASS／FAIL、対象外環境を報告する。**

### 10. 1,000件性能計測が未実施

- **重大度：🔵 提案**
- **該当箇所：`buildSortedRows()`、`computeConnectorLayout()`、`renderCompare()`、完了報告§7**

- **指摘内容：10. 最適化前の現状計測自体が行われていません。** 目標値の合意前でも現状値は取得できます。
- **機能条件：** 1,000件以上と多数の重複期間で計測し、合意した応答時間を満たすこと。
- **修正案：**
  1. **1,000件で描画時間、DOM数、操作応答、メモリを記録する。**
  2. **重い場合は時刻検索を二分探索、区間レーンを終了位置管理へ変更する。**
  3. **改善前後を同一データで比較する。**

### 11. CSP・インラインイベント・CSS整理が部分対応

- **重大度：🔵 提案**
- **該当箇所：CSP、`onclick`等、CSS全体、完了報告§8**

- **指摘内容：11. `unsafe-inline`、多数のインラインイベント、`!important`が残り、残存リスクと次の作業単位が確定していません。**
- **機能条件：** 単一HTML配布を維持しつつイベント登録を集約し、残るリスクを明記すること。
- **修正案：**
  1. **静的HTMLの`onclick`から`addEventListener`へ移す。** 影響範囲が読みやすいためです。
  2. **動的ボタンはイベント委譲へ統一する。**
  3. **script hashを検証し、不採用なら理由と残存リスクを記録する。**
  4. **`!important`は1件ずつ置換して回帰確認する。**

### 12. 完了報告が実装・検証状況と不一致

- **重大度：🟡 警告**
- **該当箇所：完了報告全体**

- **指摘内容：12. 「対応完了」とされていますが、致命的な保存経路と必須テストが未完了です。** 保存形式バージョン、テスト数、2タブ結果、復元失敗、対象ブラウザ、最大件数、性能値、構文確認結果も不足しています。
- **機能条件：** 致命的・警告級の未修正や未確認テストがあれば完了と報告しないこと。
- **修正案：**
  1. **現在の報告を「部分対応・再修正必要」へ訂正する。**
  2. **修正・テスト後、必須項目をすべて含む完了報告を再作成する。**
  3. **推奨コードとの差異は理由・効果・副作用・再利用知見まで全件記載する。**

## 実装おすすめ順

1. 🔴 一括削除・インポートを競合保護へ統合
2. 🔴 競合・保存失敗後の編集ロックを実効化
3. 🔴 復元失敗を検出して誤案内を廃止
4. 🟡 全保存経路で戻り値を確認
5. 🟡 44px・フォーカス・ラベル・ARIAを完成
6. 🟡 破損データと孤立参照の正規化を完成
7. 🟡 必須自動テストと実ブラウザ検証
8. 🔵 1,000件性能計測と必要な最適化
9. 🔵 CSP・イベント・CSSの段階整理
10. 完了報告を実測結果に合わせて再作成

## 再レビュー合格条件

- 古いタブの一括削除・インポートで新しい更新が消えない
- 競合後は再読み込み・書き出し以外を操作できない
- 途中失敗時に保存前へ戻り、復元失敗も検出できる
- 保存失敗した内容を保存済みとして表示しない
- 保存不可環境では変更操作を停止する
- 主要操作が原則44×44px以上で全操作にフォーカスが見える
- 入力名・必須状態・アイコン名・切替状態が支援技術へ伝わる
- 破損JSON・孤立人物参照でも安全に起動する
- 必須自動テスト、構文確認、`git diff --check`が成功する
- 1,000件以上で性能計測結果を記録する
- 完了報告が必須項目をすべて含む
