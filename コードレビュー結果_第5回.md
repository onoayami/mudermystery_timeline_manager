# コードレビュー結果（第5回・機能要件/エラー集照合版）

- **レビュー日**: 2026-07-25
- **対象ファイル**: マダミス時系列整理.html（全2604行 / 単一HTML・CSS・Vanilla JS・localStorage永続化）
- **照合資料**: 機能要件.md（全218行）、エラー集.md（全763行）
- **レビュー観点**: バグ・論理エラー / セキュリティ / パフォーマンス / 可読性・命名 / エラーハンドリング / 時間計算量（Big-O） / 不要ループ・再計算 / メモリ / DBクエリ効率 / キャッシュ / 「なぜ動くのか不明」箇所 / 動作しない環境 / エラー集からの予防策

---

## 0. 総評

単一HTML＋Vanilla JSとしては例外処理・エスケープ・後方互換への配慮が行き届いており、エラー集26（属性エスケープ）・27（レイアウトスラッシング）・28（時刻整形）などの過去の指摘はコードに反映済み。以下は残存する問題と予防提案。

**対応優先度（推奨順）**: 指摘1 → 指摘2 → 指摘6-修正案1（旧ブラウザ検出）→ 指摘3。
前2つは「起動しなくなる」系、指摘6は「無言の白画面」系で、いずれもユーザーにはデータ消失と区別がつかないため。

---

## 🔴 致命的

### 指摘1. `localStorage` への直接アクセスが多数残っており、ストレージ無効環境で初期化全体が停止する

- **重大度**: 🔴 致命的
- **該当箇所**: L741〜L765（`memoConcealed`・`deathFrom`・`deathTo`・`deathEnabled`・`flagFilter`・`timelineMode`・`hideConcealedInCompare`・`compareOrderFixed`・`compareZoom`・`confirmDelete`・`sessionDataExpanded`・`currentView` の初期化）、L775（`memoInput.value`）、L824（`renderSessionName` 内）
- **指摘内容**:
  **要約：機能要件§1「保存・読込は例外を握りつぶすヘルパー経由」が、boolean/文字列系の読み込み15箇所以上で守られていない。**
  `loadJSON`/`safeSet` は用意されているのに、上記の初期化と `renderSessionName()` は生の `localStorage.getItem()` を呼んでいる。Chrome系で「サイトのデータをブロック」設定時や一部の組み込みブラウザでは、`localStorage` プロパティへのアクセス自体が `SecurityError` を投げるため、L741で例外→スクリプト全体が停止→**画面が完全に無反応**になる。「プライベートモードでも停止しない」という要件に対する明確な違反。エラー集「E. localStorageの読み書き」で予見されていたリスクの未対応部分。
- **修正案**:
  1. **（推奨）読み取り用ヘルパー `safeGet` を追加し、全直接呼び出しを置換する**。書き込み側は既に `safeSet` があるため対称性も良く、変更量が最小で済む。

     ```js
     function safeGet(key, fallback = null) {
       try { const v = localStorage.getItem(key); return v === null ? fallback : v; }
       catch (e) { console.warn(`${key} の読み込みに失敗しました`, e); return fallback; }
     }
     // 例: let memoConcealed = safeGet(KEYS.memoConcealed) === 'true';
     //     let currentView = safeGet(KEYS.view) || (window.innerWidth < 900 ? 'mobile' : 'pc');
     ```

  2. 併せて `renderSessionName()`（L824）と `exportData` 内のセッション名取得（L977）も `safeGet` 経由にすると、実行中のストレージ剥奪（Safariのプライベートタブ切替等）にも耐える。

### 指摘2. 保存データの「型」を検証していないため、破損・不正なインポートデータで起動不能になる

- **重大度**: 🔴 致命的
- **該当箇所**: L725〜L733（`events`/`characters` の読み込みと後方互換マイグレーション）、L1024〜L1027（`importData` の `importable.forEach` による保存）
- **指摘内容**:
  **要約：`loadJSON` はJSONパース失敗しか防げず、「パースは通るが型が違う」値で `events.forEach`（L732）が即例外→アプリ全体が起動しない。**
  `importData` は各キーの値が `typeof v === 'string'` であることしか確認しない。例えば `madamisu_events` に `"123"` や `"{\"a\":1}"` が入ったファイルを取り込むと、`loadJSON` は数値やオブジェクトをそのまま返し、L732 の `events.forEach` が `TypeError` で停止する。取り込みは `location.reload()` で終わるため、**リロード直後から二度と起動しない**（全データ削除ボタンも押せない）状態になる。エラー集32「localStorageの複数キー更新を検証付きに」の未対応部分であり、再現性はインポート機能がある以上「条件付きで高」。
- **修正案**:
  1. **（推奨）読み込み直後に型を矯正する**。最小変更で起動不能を確実に防げる。

     ```js
     let events = loadJSON(KEYS.events, []);
     if (!Array.isArray(events)) events = [];
     events = events.filter(ev => ev && typeof ev === 'object');
     let characters = loadJSON(KEYS.characters, []);
     if (!Array.isArray(characters)) characters = [];
     characters = characters.filter(c => c && typeof c === 'object' && typeof c.name === 'string');
     ```

  2. さらに堅牢にするなら、`importData` 側で `payload[KEYS.events]` を `JSON.parse` して `Array.isArray` を確認してから保存する（不正ならそのキーだけスキップ）。バックアップ＆ロールバック機構（L1020〜L1037）は既にあるので、検証を足すだけで完成度が上がる。

---

## 🟡 警告

### 指摘3. 日付跨ぎの期間イベントで「間にある出来事」の判定が反転している

- **重大度**: 🟡 警告
- **該当箇所**: L2033〜L2043（`buildSortedRows` 内 `const lo = Math.min(s, e), hi = Math.max(s, e);` と `hasBetween` 判定）
- **指摘内容**:
  **要約：`buildSortedRows` の `lo=min(s,e), hi=max(s,e)` は、終了＜開始（日跨ぎ）の期間では「範囲外」を「範囲内」として扱う論理エラー。**
  機能要件§10は「開始＞終了は日付跨ぎとして扱う」と定めている（死亡推定時刻の `isInDeathRange`（L1714）は `t >= f || t <= to` で正しく実装済み）。しかし期間カードの分割判定（L2035）は単純に min/max で挟むため、例えば「23:00〜翌1:00」の期間に対し、**12:00の無関係な出来事が「期間の途中」と誤判定**され、開始・終了カードに分割された上、コネクタが「1:00→23:00」という逆向きの長大な縦線として描画される。リスト表示では終了カード（1:00）が開始カード（23:00）より上に並び、時系列として意味が逆転する。
- **修正案**:
  1. **（推奨）日跨ぎ期間は分割判定を跨ぎ用ロジックに分ける**：

     ```js
     const crossing = e < s;
     const hasBetween = markers.some(m => m.index !== index &&
       (crossing ? (m.t > s || m.t < e) : (m.t > s && m.t < e)));
     ```

     ただし跨ぎ期間の分割表示（0時を境に2本のコネクタを描く等）はレイアウト設計が別途必要なので、当面は「跨ぎ期間は常に単一カード表示（`hasBetween = false` に固定）」へ倒すのが安全。理由：現状の逆向きコネクタより、1枚カード表示のほうが要件（§8「単一カード表示」）と矛盾しない。
  2. 中期的には、イベントに「日オフセット」（`day: 0/1`）を持たせて分単位を `day*1440+分` に正規化すれば、ソート・範囲判定・比較ビューがすべて一貫する（エラー集32のデータモデル改善と同方向）。

### 指摘4. `timeToMinutes` が部分一致・範囲外の値を受け入れ、自由記述が意図せず時刻扱いになる

- **重大度**: 🟡 警告
- **該当箇所**: L1564〜L1569（`timeToMinutes`）、L1543〜L1561（`normalizeTime`）
- **指摘内容**:
  **要約：`match(/(\d{1,2}):(\d{2})/)` は文字列中のどこでもマッチし、時・分の上限も検証しないため、「翌1:00」「第2章10:30頃」等の自由記述が時刻として先頭側にソートされる。**
  機能要件§7は「自由記述は末尾にまとまる」だが、「翌1:00」は 1:00（=60分）として**深夜1時の位置**に並ぶ。日跨ぎシナリオが多いマダミスでは実際に入力されやすい表記。また `normalizeTime` は `10:75`→そのまま（675分=11:15相当の位置に無言でソート）、`77:99` も許容する。
- **修正案**:
  1. **（推奨）アンカー付き・範囲検証ありに強化**：

     ```js
     function timeToMinutes(t) {
       if (!t) return Infinity;
       const m = String(t).trim().match(/^(\d{1,2}):(\d{2})$/);
       if (!m) return Infinity;
       const h = +m[1], min = +m[2];
       if (min > 59) return Infinity;   // 分の異常値は自由記述扱い
       return h * 60 + min;             // 25:00 等の「翌」表現を許容するなら h の上限は付けない
     }
     ```

     理由：完全一致にするだけで「翌1:00」「10:30頃」が自由記述（末尾）に落ち、要件の記述と一致する。
  2. `normalizeTime` の blur 時に `分>59` を検出して `alert` や赤枠で知らせると、`10:75` のような入力ミスの早期発見になる（要件の「入力ミス防止」趣旨に合致）。

### 指摘5. 「時間=自由記述＋終了時刻あり」のデータが不可視のまま保存される（`isRangeEvent` の依存問題）

- **重大度**: 🟡 警告
- **該当箇所**: L1998〜L2001（`isRangeEvent`）、L1770付近（`addEvent` の期間モード保存処理）
- **指摘内容**:
  **要約：開始時刻を「夕方」等にして期間モードで追加すると、`endTime` は保存されるが `isRangeEvent` が false のため、UI上どこにも表示・編集できない“幽霊データ”になる。**
  リスト表示は単一カード扱い（終了時刻非表示）、✏️編集フォームには `edit-endtime` が出る（値は見える）ので完全に閉じてはいないが、比較ビューや書き出しデータに説明のつかない `endTime` が残り続ける。
- **修正案**:
  - **（推奨）`addEvent` 側で入り口を塞ぐ**：期間モード時に開始時刻が時刻形式でなければ `alert('期間で登録するには開始時刻を HH:MM 形式で入力してください')` で弾く。理由：データの整合性を入力時点で保証でき、後段の表示ロジックに分岐を足さずに済む。
  - 代替：`isRangeEvent` を「endTimeがあれば期間」とみなし、開始が自由記述の期間は「不詳〜終了」として末尾に表示する（仕様追加になるため要件確認が必要）。

### 指摘6. CSS `zoom`・`display: contents`・オプショナルチェーンなど環境依存で全滅または一部機能が壊れる構文がある

- **重大度**: 🟡 警告
- **該当箇所**: L1665（`wrap.style.zoom`）、L405（`display: contents`）、L1109・L1484・L1519（オプショナルチェーン `?.`）
- **指摘内容**:
  **要約：①Firefox 126未満ではズームが無反応、②古いiOS Safari（13.4未満）では `?.` が構文エラーで**スクリプト全体が1行も動かない**、③`display: contents` は古いSafariでレイアウト崩れ・支援技術の読み上げ欠落の既知バグがある。**
  エラー集「F. 境界値・互換性」で①は既知。②が最も影響が大きく、構文エラーはパース時に発生するため `try/catch` でも防げず、白画面と区別がつかない「無言の全停止」になる（エラー集23で経験済みの症状と同型）。iPadでの利用が想定されている（エラー集14）ため無視できない。
- **修正案**:
  1. **（推奨）対象ブラウザの下限を明文化**し、`<script>` の直後に機能検出＋案内を置く：

     ```html
     <script>
       // 旧ブラウザでは後続のスクリプトがパースエラーで全停止するため、先に検出して案内する
       try { new Function('a', 'return a?.b'); }
       catch (e) { document.body.innerHTML = '<p style="padding:2em">このアプリは新しいブラウザ（iOS 13.4以降のSafari等）でご利用ください。</p>'; }
     </script>
     ```

     ※検出スクリプト自体は旧構文のみで書くこと。理由：コードを書き換えずに「無言の白画面」を防げる。
  2. ズームは `zoom` 非対応時のフォールバックとして、`CSS.supports('zoom','1')` が偽なら `transform: scale()` ではなく（エラー集6で回避指定）`font-size` ベースの縮小、または「この環境では倍率変更不可」の表示に切り替える。
  3. `?.` を使っている3箇所（L1109 / L1484 / L1519）は `p.closest(...)` の結果を変数に受けて `if` で分岐する旧構文に書き換えれば、少なくとも構文エラー起因の全停止だけは解消できる（②の根治）。

### 指摘7. インライン `onclick` に配列 index を焼き込む設計は、部分更新（`refreshCard`）拡大と併存すると誤対象操作の温床になる

- **重大度**: 🟡 警告
- **該当箇所**: L1735（`refreshCard`）、L2220〜L2260付近（`renderEvents` 内の `onclick="deleteEvent(${index})"` 等）、L1849（`startCompareEdit`）
- **指摘内容**:
  **要約：現状は「変更→即・全再描画」の規律で index の鮮度が保たれており実害はないが、これは“たまたま動いている”状態（レビュー観点11に該当）。**
  エラー集「E. 配列indexをイベント識別子として使う設計」で自己指摘済み。`refreshCard` による部分更新が既に導入されており、今後「並べ替えを伴う操作＋部分更新」の組み合わせを1つ追加しただけで、隣のカードを削除するバグが顕在化する。特に `startCompareEdit`（L1849）は編集フォームに index を埋め込むため、編集中に別カードで `deleteEvent` すると保存先がズレる可能性がある（現状は削除が全再描画で編集フォームごと破棄されるため偶然セーフ）。
- **修正案**:
  1. **（推奨）各イベントに不変ID（`id: crypto.randomUUID()` または連番）を付与**し、DOMは `data-event-id`、ハンドラは ID→`events.findIndex` で解決する。エラー集25・32でも繰り返し推奨されている根本対策で、キャラ名の識別子問題（`compareOrder` が名前ベース）も同時に解決の道筋がつく。
  2. 暫定策：`sortEvents()` を呼ぶ関数（`saveEditTime`/`saveEditEvent`/`moveUnknownEvent`/`addEvent`）では必ず全再描画する現在の規律をコメントとして明文化し、「sort後にrefreshCardを呼んではならない」ことを将来の変更者に伝える。

---

## 🔵 提案

### 指摘8. `hideConcealedInCompare` と保存キー `show_concealed_compare` の意味が反転しており可読性を損なう

- **重大度**: 🔵 提案
- **該当箇所**: L756（変数宣言）、L1690付近（`toggleShowConcealedCompare`）
- **指摘内容**:
  **要約：変数は「隠す」、キー名は「表示する」、チェックボックスIDは `showConcealedCompareToggle`、ラベルは「不可視化」と、同一状態に4通りの語彙が混在。**
  true が「隠す」であることを毎回コードを追って確認する必要があり、将来の修正ミス（真偽反転）の典型的な発生源。
- **修正案**: 変数・関数・要素IDを `hideConcealedInCompare` 系に統一（キー名は保存互換のため据え置き、コメントで「歴史的経緯でshowだが意味はhide」と明記）。localStorage キーを変えないため移行処理は不要。

### 指摘9. `buildSortedRows` の分割判定が O(R×M)、比較ビューのレーン占有判定が O(レーン×行×スパン)

- **重大度**: 🔵 提案
- **該当箇所**: L2035（`hasBetween`）、L2440付近（`renderCompare` の `colLanes` 構築）
- **指摘内容**:
  **要約：イベント数nに対し最悪 O(n²) の箇所が2つあるが、マダミスの実用規模（数十〜200件）では実測影響なし。**
  ただし `renderEvents` が「1文字入力以外のほぼ全操作」で全再実行される設計のため、将来500件超のデータを取り込んだ場合、比較ビューの描画がフレーム落ちする可能性がある。
- **修正案**:
  - markersを時刻でソートして二分探索（`hasBetween` を O(log M)）にすれば O(n log n) 化できるが、**現時点では実装せず**「想定上限〜300件。超える場合はここを最適化」とコメントを置くだけで十分（過剰最適化の回避）。
  - `colorMap`/`indexMap`（`renderEvents` 冒頭）と `normalizedCompareOrder` は毎描画で再構築される。キャッシュ余地はあるが、無効化タイミング管理のバグリスクの方が大きいため、現状維持を推奨（観点10への回答：**意図的にキャッシュしない判断が妥当**）。

### 指摘10. `events` に残る `character`（単数）・`color` フィールドは描画で参照されない実質デッドデータ

- **重大度**: 🔵 提案
- **該当箇所**: L1514（`toggleCardChar` 内の `events[index].color = ...`）、`addEvent` の `color:` 設定
- **指摘内容**:
  **要約：カード色は常に `characters` 配列＋`colorForChar()` から動的に引くため、保存される `ev.color` は表示に使われず、キャラの色変更後は古い色のまま残る「嘘のデータ」になる。**
  後方互換のための `character`（単数）は妥当だが、`color` は互換にも使われておらず、書き出しJSONを読む人を誤解させる。
- **修正案**: 新規保存時に `color` の書き込みを止める（読み込み側は元々参照していないので互換影響なし）。`character` は旧版読み込み互換のため維持で良い。

### 指摘11. 「なぜ動くのか分かりにくい」暗黙結合が3箇所（観点11）

- **重大度**: 🔵 提案
- **該当箇所**:
  - L399〜L406：スマホ版の `display: contents` — `timeline-item-inner` と `event-body` の箱を消して親flexに直接並べ、`order` で再配置。`z-index` が効かなくなる副作用を個別指定（L408〜L412）で埋めており、1つ消すとコネクタが前面に出る。
  - L246〜L253：`in-death-range + in-death-range::before { top:-9px }` は `.timeline-item { margin-bottom:8px }` と1px重ねの合算に依存（エラー集の総括でも「回帰しやすい」と明記済み）。
  - L905付近（`applyView`）：`view-pc` クラスを**一旦当ててから**同期リフローで `pcModeToggleWraps()` を測る手法。動作は正しいが、将来ここに非同期処理を挟むと一瞬PCレイアウトが見える（FOUC）恐れがある。
- **修正案**: いずれも動作は正常なので書き換え不要。**依存関係をCSS変数で明示**するのが低リスクな改善（例：`--card-gap: 8px` を定義し、`margin-bottom` と `top: calc(-1 * var(--card-gap) - 1px)` の両方で使う）。1箇所の変更が連動し、エラー集で繰り返された「赤バー分断」回帰を構造的に防げる。

### 指摘12. エラー集由来の予防策（観点13）— 未対応・部分対応の項目

- **重大度**: 🔵 提案
- **該当箇所**: 全体
- **指摘内容と修正案**:
  1. **iPadの外側タップ問題（エラー集14）**: `body{cursor:pointer}` ハックで部分対応済みだが、エラー集自身が推奨する `pointerdown` 委譲＋`closeAllPopups()` 一元化が未実装。現状 `closeAllCharPopups()`（人物系）とカラーパレット閉鎖（L1097〜L1111）が別ロジックで、閉じ漏れの温床。→ **document の `pointerdown` で全ポップアップを1関数で閉じる**方式への統合を推奨（スクロール誤発火は `pointerdown`→`pointerup` の移動量判定で回避）。
  2. **検証データの本番混入（エラー集17）**: 予防機構なし。→ URLハッシュ `#test` のときだけ保存キーに `_test` サフィックスを付ける5行程度の仕組みで、開発時の事故を構造的に防げる。
  3. **比較ビューの⬆⬇が「動かないように見える」（エラー集9）**: `moveUnknownEvent` は全カード基準（`buildSortedRows(false)`）で移動するため、絞り込み中は1回押しても表示が変わらないことがある。→ 移動後に隣接判定を「表示中の行」基準で再試行するか、動かなかった場合にボタンを一瞬点滅させるなどのフィードバック追加を推奨。
  4. **インポート途中失敗（エラー集E）**: バックアップ＆ロールバック実装済み（L1020〜L1037）で**対応完了**。ただし指摘2の型検証を足すと完全になる。
  5. **`file://` 検証の混乱（エラー集18）**: アプリ側対応は不要だが、開発時は `python3 -m http.server` 等の利用をREADMEに一行追記すると再発予防になる。

### 指摘13. `lockCharacter`/`unlockCharacter` の二重描画と、ロックが `recolorCharacter` で検証されない点

- **重大度**: 🔵 提案
- **該当箇所**: L1175〜L1186（`lockCharacter`/`unlockCharacter`）、L1263付近（`recolorCharacter`）
- **指摘内容**:
  **要約：①`saveCharacters()` が内部で `renderCharacters()` を呼ぶため、直後の明示呼び出しは重複（実害なしの無駄描画）。②ロックは「一覧UIのボタン非表示＋`startEditCharacter`/`deleteCharacter` 冒頭ガード」で守られているが、`recolorCharacter` にはガードがなく、理論上パレットDOMが残っていれば色変更できる。**
- **修正案**:
  - `lockCharacter`/`unlockCharacter` の末尾 `renderCharacters()` を削除（1行）。
  - `recolorCharacter` 冒頭に `if (characters[index].locked) return;` を追加。要件§5「名前・色の編集と削除を防ぐ」の「色」の防御が関数レベルで揃う。

### 指摘14. セッション名編集が「フォーカスを外しても編集モードのまま残る」

- **重大度**: 🔵 提案
- **該当箇所**: L840〜L878付近（`startSessionNameEdit`〜`finishSessionNameEdit`）
- **指摘内容**:
  **要約：確定手段が✓とEnter×2のみで、blur時に何も起きないため、編集を始めて他所をタップしたユーザーには「保存されたのか放置なのか」が分からない。**
  要件§2には blur の挙動が未定義なので違反ではないが、スマホでは特に迷いやすい挙動。
- **修正案**: `sessionNameInput.addEventListener('blur', ...)` で**キャンセル（表示モードへ戻す・保存しない）**を推奨。保存にすると誤タップ確定が起きるため、「明示操作のみ保存」という現行思想と整合するのはキャンセル側（✎ボタンの `onmousedown="event.preventDefault()"` が既にあるので✓押下と競合しない）。

---

## 観点別サマリ

| 観点 | 結果 |
|---|---|
| 1. バグ・論理エラー | 指摘3（日跨ぎ期間・要修正）、指摘4・5（時刻判定の緩さ） |
| 2. セキュリティ | `escapeHtml`（クォート含む）・`safeColor`・インデックス由来クラス名で概ね良好。残リスクは指摘2（インポート型検証）のみ |
| 3. パフォーマンス | 全再描画方式だが規模的に妥当。指摘9参照 |
| 4. 可読性・命名 | 指摘8（show/hide反転）、指摘10（デッドデータ） |
| 5. エラーハンドリング | `safeSet`/ロールバックは良好。**読み取り側が穴**（指摘1・2） |
| 6. 時間計算量（Big-O） | 最悪 O(n²) 2箇所、実用規模では許容（指摘9） |
| 7. 不要ループ・再計算 | `lockCharacter` の二重描画（指摘13）程度 |
| 8. メモリ使用量 | 問題なし（リスナーは委譲中心、リーク経路なし） |
| 9. DBクエリ効率（N+1等） | 該当なし（localStorageのみ）。複数キー更新の原子性は指摘2・指摘12-4でカバー |
| 10. キャッシュ活用 | 意図的な非キャッシュが妥当（指摘9） |
| 11. 偶然動いている箇所 | 指摘7（index規律）、指摘11（display:contents／-9px結合／同期リフロー計測） |
| 12. 動かない環境 | 指摘6（旧Safariの `?.` で全停止＝最重要、Firefox<126 の zoom、古いSafariの display:contents） |
| 13. エラー集からの予防 | 指摘12参照（iPadタップ・テストデータ分離・⬆⬇フィードバックが未対応） |

---

## 修正済みであることを確認した過去指摘（参考）

- `escapeHtml` は `& < > " '` の5文字を変換し、属性値コンテキストでも安全（エラー集26対応済み）
- 色は `safeColor()` で `#RRGGBB` のみ許可し、インラインstyle・動的CSSに一貫適用
- 動的CSSはキャラ名ではなく `char-idx-N`（インデックス由来）でCSSインジェクションを回避
- 比較ビューは事前構築した `indexOfEv` マップでセルごとの `indexOf`（O(n²)）を排除
- `alignControlsToShortestCard` は「読み取り一括→書き込み一括」に分離しレイアウトスラッシングを削減（エラー集27対応済み）
- 比較ズームの下限バグ（エラー集5）は `clampCompareZoom` の `isFinite` ＋ min/max で修正済み
- インポートのバックアップ＆ロールバック（エラー集E）は実装済み
- 時刻整形の3桁/4桁競合（エラー集28）は「入力中は4桁のみ即整形、3桁はblur時」で対応済み
- 比較順とキャラ一覧順の分離（エラー集25）は `compareOrder` ＋ 名前変更時の追従で対応済み
